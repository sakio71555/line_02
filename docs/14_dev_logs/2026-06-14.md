# 2026-06-14 Development Log

## Loop 021: Supabase client boundary

### Date
2026-06-14

### What changed
- `packages/db` にserver-side Supabase client boundaryを追加。
- env validationを `readSupabaseConfigFromEnv` に集約。
- service role clientとanon clientの作成関数を分けた。
- importしただけではenv validationやDB接続をしないunit testを追加。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- lint / typecheck / test / test:integration 成功。
- 17 files / 92 tests passed。

### Tenant isolation
- Supabase repository実装はまだ未接続。
- 後続repositoryでは `tenant_id` 条件必須とする前提を維持。

### External API
- Supabase本番接続なし。
- LINE APIなし。
- OpenAI APIなし。

### Risks
- Supabase repository、RLS、local migration testは後続Loop。

### Next
- Loop 022: Supabase customer/message repository

## Loop 022: Supabase customer/message repository

### Date
2026-06-14

### What changed
- `SupabaseCustomerRepository` を追加。
- `SupabaseMessageRepository` を追加。
- Supabase repository error wrapperを追加。
- fake Supabase clientでtenant_id条件、返却型、error handlingを確認するtestを追加。
- `@amami-line-crm/db` にworkspace内部依存として `@amami-line-crm/domain` を追加。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- lint / typecheck / test / test:integration 成功。
- 18 files / 102 tests passed。

### Tenant isolation
- customer/message queryは `tenant_id` 条件必須。
- fake client testで別tenant rowを返さないことを確認する。

### External API
- Supabase本番接続なし。
- LINE APIなし。
- OpenAI APIなし。

### Risks
- `customers.last_customer_message_at` はdomainにあるがmigration未反映。API接続前にschema syncが必要。
- Supabase repositoryはまだ既存API routeへ接続していない。

### Next
- Loop 023: Supabase alert repository

## Loop 023: Supabase alert repository

### Date
2026-06-14

### What changed
- `SupabaseAlertRepository` を追加。
- alert create/list/open/active/updateStatusのSupabase mappingを追加。
- fake Supabase clientでtenant_id条件、status条件、error handlingを確認するtestを追加。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- lint / typecheck / test / test:integration 成功。
- 19 files / 112 tests passed。

### Tenant isolation
- alert queryは `tenant_id` 条件必須。
- open alertは `tenant_id + status = open` で絞る。
- updateStatusは `tenant_id + alert_id` で絞る。

### External API
- Supabase本番接続なし。
- LINE APIなし。
- OpenAI APIなし。

### Risks
- Supabase AlertRepositoryはまだ既存API routeへ接続していない。

### Next
- Loop 024: Supabase knowledge repository

## Loop 022.1: customer schema sync

### Date
2026-06-14

### What changed
- `customers.last_customer_message_at` を初期migrationへ追加。
- `SupabaseCustomerRepository` のcustomer write mappingに `last_customer_message_at` を追加。
- schema testとSupabase customer repository testを更新。
- Loop 022 docsのschema/domain差異を解消済みに更新。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- lint / typecheck / test / test:integration 成功。
- 18 files / 103 tests passed。

### Tenant isolation
- `customers` tableは引き続き `tenant_id` 必須。
- Supabase customer upsert payloadは `tenant_id` と `last_customer_message_at` を含む。

### External API
- Supabase本番接続なし。
- LINE APIなし。
- OpenAI APIなし。

### Risks
- 既存API routeはまだSupabase repositoryへ接続していない。

### Next
- Loop 023: Supabase alert repository

## Loop 024: Supabase knowledge repository

### Date
2026-06-14

### What changed
- `SupabaseKnowledgePageRepository` を追加。
- `knowledge_pages` の `tenant_id` / `allowed_for_ai` filterとdomain mappingを追加。
- fake Supabase clientでtenant_id条件、allowed_for_ai条件、RAG interface互換性を確認するtestを追加。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- lint / typecheck / test / test:integration 成功。
- 20 files / 118 tests passed。

### Tenant isolation
- knowledge queryは `tenant_id` 条件必須。
- 返却rowも防御的に `tenant_id` でfilterする。

### External API
- Supabase本番接続なし。
- LINE APIなし。
- OpenAI APIなし。

### Risks
- Supabase KnowledgePageRepositoryはまだ既存API routeやRAG実行経路へ接続していない。

### Next
- Loop 025: Supabase RLS policy plan

## Loop 025: Supabase RLS policy plan

### Date
2026-06-14

### What changed
- Supabase RLS policy planをdocs-onlyで追加。
- table別policy matrix、access pattern、key運用、RLS test方針を整理。
- READMEとdatabase docsにRLSはまだ計画段階であることを追記。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- lint / typecheck / test / test:integration 成功。
- 20 files / 118 tests passed。

### Tenant isolation
- tenant-owned tableはRLS対象として `tenant_id` 分離方針を整理。
- service role利用時もrepository側 `tenant_id` filter testを残す方針。

### External API
- Supabase本番接続なし。
- LINE APIなし。
- OpenAI APIなし。

### Risks
- RLS SQL、auth/staff context、local RLS testは後続Loop。

### Next
- Loop 026: Supabase local migration test

## Loop 026: Supabase local migration test

### Date
2026-06-14

### What changed
- Supabase local migration testのrunbookを追加。
- local toolsを確認し、Docker daemon unavailable / `psql` missingのため実DB適用は未実行と記録。
- database schema validation testに主要indexとRLS SQL未混入の確認を追加。

### Commands
- `git status --short`
- `command -v supabase || true`
- `command -v docker || true`
- `command -v psql || true`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- lint / typecheck / test / test:integration 成功。
- 20 files / 119 tests passed。

### Tenant isolation
- tenant-owned tableと主要indexのSQL validationを強化。
- RLS SQLはまだ未実装で、専用Loopまで初期migrationに混ぜない方針を確認。

### External API
- Supabase本番接続なし。
- LINE APIなし。
- OpenAI APIなし。

### Risks
- local migration applyは未実行。
- Docker daemon起動または安全なlocal PostgreSQL環境が必要。

### Next
- Loop 027: Supabase auth/staff tenant context plan

## Loop 027: Supabase auth/staff tenant context plan

### Date
2026-06-14

### What changed
- 本番向けstaff/admin tenant context planをdocs-onlyで追加。
- 現在の `x-tenant-id`、LINE webhook tenant解決、demo seedの扱いを整理。
- Supabase Auth / JWT、role / permission、RLS planとの接続を整理。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- lint / typecheck / test / test:integration 成功。
- 20 files / 119 tests passed。

### Tenant isolation
- 本番admin APIでは `x-tenant-id` を信用せず、認証済みstaff membershipからtenant contextを決定する方針。
- tenant_id未確定のままrepositoryを呼ばない方針。

### External API
- Supabase本番接続なし。
- LINE APIなし。
- OpenAI APIなし。

### Risks
- Supabase Auth、JWT検証、auth_user_id/membership schema、admin login UIは未実装。

### Next
- Loop 028: staff tenant schema plan

## Loop 028: staff tenant schema plan

### Date
2026-06-14

### What changed
- staff/admin tenant schema planをdocs-onlyで追加。
- 現在のtenant-scoped `staff_users` と不足している `auth_user_id` / membership / statusを整理。
- target schemaとしてstaff identity、staff tenant membership、invite、role / permission / status方針を整理。
- README、database docs、dev loop docsにLoop 028の計画段階を追記。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- lint / typecheck / test / test:integration 成功。
- 20 files / 119 tests passed。

### Tenant isolation
- 本番admin APIでは `x-tenant-id` を信用せず、`auth_user_id` とactive membershipからtenant contextを決定する設計。
- RLS SQL前にstaff identity / membership schemaを決める方針を明記。

### External API
- Supabase本番接続なし。
- LINE APIなし。
- OpenAI APIなし。

### Risks
- migration SQL、Supabase Auth、JWT検証、membership table、RLS SQLは未実装。
- 現在のruntimeは引き続きin-memoryで、admin routeはdev-only `x-tenant-id` を使う。

### Next
- Loop 029: staff tenant schema migration

## Loop 029: staff tenant schema migration

### Date
2026-06-14

### What changed
- `staff_users` に `auth_user_id`、`status`、`disabled_at`、`archived_at` を追加。
- `staff_tenant_memberships` tableを追加し、tenantごとのrole/statusを表現する準備をした。
- staff role/statusのdomain typeと軽いZod schemaを追加。
- database schema validation test、README、database docs、dev loop docsを更新。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- lint / typecheck / test / test:integration 成功。
- 20 files / 120 tests passed。

### Tenant isolation
- `staff_tenant_memberships` は `tenant_id` 必須で、`tenant_id + staff_user_id` を一意にする。
- 本番tenant contextは将来active membershipから決定する方針を維持。

### External API
- Supabase本番接続なし。
- LINE APIなし。
- OpenAI APIなし。

### Risks
- Supabase Auth、JWT検証、API tenant guard、RLS SQLは未実装。
- `staff_users.tenant_id` は互換性のため残しており、完全なidentity分離は後続Loopで扱う。

### Next
- Loop 030: auth context boundary

## Loop 030: auth context boundary

### Date
2026-06-14

### What changed
- `packages/domain` にauth context boundaryを追加。
- `auth_user_id` からactive staff / active membershipを検証してtenant contextを返すresolverを追加。
- selected tenantをmembershipで再検証するロジックを追加。
- fake lookupによるauth context boundary testを追加。
- README、database docs、dev loop docsを更新。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- lint / typecheck / test / test:integration 成功。
- 21 files / 132 tests passed。

### Tenant isolation
- selected tenantは信用せず、active membershipに含まれる場合だけtenant contextへ採用。
- roleはmembership由来。
- tenant_id未確定のままrepositoryへ進ませないresolver方針。

### External API
- Supabase本番接続なし。
- LINE APIなし。
- OpenAI APIなし。

### Risks
- 既存Admin API routeはまだdev-only `x-tenant-id` を使う。
- Supabase Auth、JWT検証、middleware、API tenant guard、RLS SQLは未実装。

### Next
- Loop 031: admin API tenant context guard

## Loop 031: admin API tenant context guard

### Date
2026-06-14

### What changed
- `apps/api` にAdmin API tenant context guardを追加。
- 既存dev-only `x-tenant-id` 解決をguard境界へ集約。
- 既存Admin API routeとdev seed routeのレスポンス互換を維持。
- guard error code / HTTP mapping testを追加。
- README、database docs、dev loop docsを更新。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- lint / typecheck / test / test:integration 成功。
- 22 files / 137 tests passed。

### Tenant isolation
- known dev tenantは `source = dev_header` のAdminTenantContextへ集約。
- unknown/missing tenantは従来通り拒否。
- 将来のauthenticated staff pathは別sourceとして残す。

### External API
- Supabase本番接続なし。
- LINE APIなし。
- OpenAI APIなし。

### Risks
- Supabase Auth、JWT検証、Admin login、RLS SQLは未実装。
- productionでdev headerを信用しない実装切替は後続Loop。

### Next
- Loop 032: admin auth placeholder UI plan

## Loop 032: admin auth placeholder UI plan

### Date
2026-06-14

### What changed
- Admin auth placeholder UI planをdocs-onlyで追加。
- 現在のAdmin UI構成、dev-only `x-tenant-id` 付与、将来のlogin / tenant selection / permission denied / session expired導線を整理。
- role別UI方針、Admin API guardとの接続、Supabase Auth方針、placeholder UI方針を整理。
- README、database docs、dev loop docsを更新。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- lint / typecheck / test / test:integration 成功。
- 22 files / 137 tests passed。

### Tenant isolation
- 本番では `x-tenant-id` を信用せず、authenticated staff membershipからtenant contextを決定するUI/API接続方針を整理。
- 複数tenant所属時はtenant selectionを挟み、選択tenantはmembershipで再検証する方針。

### External API
- Supabase本番接続なし。
- LINE APIなし。
- OpenAI APIなし。

### Risks
- Admin login UI、Supabase Auth、JWT検証、auth-specific API error mappingは未実装。
- runtimeは引き続きdev-only `x-tenant-id` pathを使う。

### Next
- Loop 033: admin auth UI placeholder

## Loop 033: admin auth UI placeholder

### Date
2026-06-14

### What changed
- Admin auth placeholder UIを追加。
- `/login`、`/select-tenant`、`/permission-denied`、`/session-expired` を追加。
- Adminトップからplaceholder画面への導線を追加。
- placeholder page render testを追加。
- README、dev loop docs、Loop 033 task docを更新。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`

### Result
- lint / typecheck / test / test:integration / build 成功。
- 23 files / 141 tests passed。

### Tenant isolation
- 既存dev-only `x-tenant-id` MVP導線は維持。
- placeholderではtenant選択保存やmembership検証を実装せず、本番ではmembership再検証が必要な方針を表示。

### External API
- Supabase Auth接続なし。
- Supabase本番接続なし。
- LINE APIなし。
- OpenAI APIなし。

### Risks
- placeholder画面は認証を強制しない。
- Supabase Auth、JWT/session、Admin API authenticated_staff guard、role-based guardは未実装。

### Next
- Loop 034: admin API auth error response mapping

## Loop 034: admin API auth error response mapping

### Date
2026-06-14

### What changed
- Admin API auth error response mapperを追加。
- `missing_tenant_context` / `unknown_tenant` のlegacy response互換を維持。
- 将来用のauthenticated staff、session expired、tenant selection、permission系error mappingを整理。
- Admin tenant context guardのHTTP mappingを新mapperへ委譲。
- README、dev loop docs、Loop 034 task docを更新。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- lint / typecheck / test / test:integration 成功。
- 24 files / 145 tests passed。

### Tenant isolation
- 既存dev-only `x-tenant-id` guardは維持。
- unknown/missing tenantのlegacy status/bodyを変えず、将来のauthenticated_staff path用error mappingを追加。

### External API
- Supabase Auth接続なし。
- Supabase本番接続なし。
- LINE APIなし。
- OpenAI APIなし。

### Risks
- Admin UI redirect、Supabase Auth、JWT/session、authenticated_staff guard、role-based guardは未実装。

### Next
- Loop 035: Supabase Auth client boundary

## Loop 035: Supabase Auth client boundary

### Date
2026-06-14

### What changed
- Supabase Auth config boundaryを追加。
- Supabase Auth server/browser client factoryを追加。
- Auth boundaryでは `SUPABASE_URL` / `SUPABASE_ANON_KEY` のみを扱い、service role keyを含めない方針を明確化。
- fake envとfetch mockのtestを追加。
- README、database docs、dev loop docs、Loop 035 task docを更新。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- lint / typecheck / test / test:integration 成功。
- 25 files / 154 tests passed。

### Tenant isolation
- runtimeのtenant解決は変更せず、既存dev-only `x-tenant-id` pathを維持。
- 将来のauthenticated_staff guardへ接続する前段のAuth client boundaryのみ追加。

### External API
- Supabase Auth API呼び出しなし。
- Supabase本番接続なし。
- LINE APIなし。
- OpenAI APIなし。

### Risks
- Admin login、logout、session保存、JWT検証、authenticated_staff guard接続は未実装。
- Browser Auth clientのNext.js integrationは後続Loopで安全に設計する必要がある。

### Next
- Loop 036: staff auth lookup repository

## Loop 036: staff auth lookup repository

### Date
2026-06-14

### What changed
- SupabaseStaffAuthLookupRepositoryを追加。
- `auth_user_id` から `staff_users` を取得するquery境界を追加。
- `staff_user_id` から `staff_tenant_memberships` を取得するquery境界を追加。
- repositoryをLoop 030の `resolveAuthenticatedTenantContext` と組み合わせられることをtest。
- README、database docs、dev loop docs、Loop 036 task docを更新。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- lint / typecheck / test / test:integration 成功。
- 26 files / 164 tests passed。

### Tenant isolation
- Admin API runtimeのtenant解決は変更せず、既存dev-only `x-tenant-id` pathを維持。
- membership lookupは複数tenant所属を返し、tenant access判定はresolverへ委譲。

### External API
- Supabase本番接続なし。
- Supabase Auth/JWT/session取得なし。
- LINE APIなし。
- OpenAI APIなし。

### Risks
- Admin API authenticated_staff guardには未接続。
- JWT/session検証、RLS SQL、runtime Supabase接続は未実装。

### Next
- Loop 037: authenticated staff tenant guard

## Loop 037: authenticated staff tenant guard

### Date
2026-06-14

### What changed
- authenticated staff tenant guard境界を追加。
- `AuthUserIdentity + StaffAuthLookup` から `AdminTenantContext(source: authenticated_staff)` を作る関数を追加。
- domain auth context errorをAdmin auth error mapperへ渡せるhelperを追加。
- fake `StaffAuthLookup` によるguard testを追加。
- README、database docs、dev loop docs、Loop 037 task docを更新。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- lint / typecheck / test / test:integration 成功。
- 27 files / 175 tests passed。

### Tenant isolation
- selected tenantはmembershipで再検証する境界を追加。
- runtime routeはまだdev-only `x-tenant-id` pathのまま維持。

### External API
- Supabase本番接続なし。
- Supabase Auth/JWT/session取得なし。
- LINE APIなし。
- OpenAI APIなし。

### Risks
- Admin API routesには未接続。
- JWT/session検証、RLS SQL、runtime Supabase接続は未実装。

### Next
- Loop 038: admin login UI integration

## Loop 038: admin login UI integration

### Date
2026-06-14

### What changed
- `/login` を将来のAuth接続に向けた静的login UIへ整理。
- email/password入力欄とdisabled submitを追加。
- Supabase Auth未接続、送信処理なし、入力内容未保存を画面に明記。
- admin auth placeholder render testを更新。
- README、dev loop docs、Loop 038 task docを更新。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`

### Result
- lint / typecheck / test / test:integration / build 成功。
- 27 files / 175 tests passed。

### Tenant isolation
- runtime routeはまだdev-only `x-tenant-id` pathのまま維持。
- login UIはtenant contextを作らず、tenant選択保存も行わない。

### External API
- Supabase Auth API呼び出しなし。
- Supabase本番接続なし。
- LINE APIなし。
- OpenAI APIなし。

### Risks
- `/login` はまだ認証を実行しない。
- JWT/session検証、Admin API authenticated_staff guard接続、route protectionは未実装。

### Next
- Loop 039: tenant selection UI

## Loop 039: tenant selection UI

### Date
2026-06-14

### What changed
- `/select-tenant` を将来の複数tenant所属staff向けの静的tenant selection UIへ整理。
- `tenant_amamihome` のplaceholder cardとdisabled選択buttonを追加。
- tenant保存なし、API取得なし、Supabase Auth/JWT未接続を画面に明記。
- admin auth placeholder render test、README、dev loop docs、Loop 039 task docを更新。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`

### Result
- lint / typecheck / test / test:integration / build 成功。
- 27 files / 175 tests passed。

### Tenant isolation
- runtime routeはまだdev-only `x-tenant-id` pathのまま維持。
- `/select-tenant` はtenant contextを作らず、選択tenantも保存しない。

### External API
- Supabase Auth API呼び出しなし。
- tenant一覧API取得なし。
- Supabase本番接続なし。
- LINE APIなし。
- OpenAI APIなし。

### Risks
- `/select-tenant` はまだstaff membershipを取得しない。
- selectedTenantId保存、JWT/session検証、Admin API authenticated_staff guard接続は未実装。

### Next
- Loop 040: role-based admin action guard plan

## Loop 040: role-based admin action guard plan

### Date
2026-06-14

### What changed
- role-based admin action guardのdocs-only計画を追加。
- 現在のAdmin API操作一覧とAdmin UI操作一覧を整理。
- `owner` / `manager` / `staff` のrole定義と操作許可matrixを整理。
- API側guard、UI側表示制御、`permission_denied`、`/permission-denied` placeholderとの関係を整理。
- README、dev loop docs、Loop 040 task docを更新。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- lint / typecheck / test / test:integration 成功。
- 27 files / 175 tests passed。

### Tenant isolation
- runtime routeはまだdev-only `x-tenant-id` pathのまま維持。
- role guardは `authenticated_staff` context接続後に有効化する方針。

### External API
- Supabase Auth/JWT接続なし。
- Supabase本番接続なし。
- LINE APIなし。
- OpenAI APIなし。

### Risks
- role guard実装、UI制御、Admin API runtime接続は未実装。
- `viewer` / `platform_admin` はschema/type未追加。

### Next
- Loop 041: admin role permission boundary

## Loop 041: admin role permission boundary

### Date
2026-06-14

### What changed
- `packages/domain` にAdmin role permission boundaryを追加。
- `owner` / `manager` / `staff` と `AdminAction` の許可判定をpure function化。
- default deny、unknown role/action、`permission_denied` に変換しやすいresult/throw境界を追加。
- role matrix test、README、dev loop docs、Loop 041 task docを更新。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- lint / typecheck / test / test:integration 成功。
- 28 files / 184 tests passed。

### Tenant isolation
- runtime routeはまだdev-only `x-tenant-id` pathのまま維持。
- permission boundaryはrole文字列だけを受け取り、tenant contextやDBには接続しない。

### External API
- Supabase Auth/JWT接続なし。
- Supabase本番接続なし。
- LINE APIなし。
- OpenAI APIなし。

### Risks
- Admin API route guard、Admin UI制御、authenticated runtime接続は未実装。
- `viewer` / `platform_admin` はschema/type未追加。

### Next
- Loop 042: admin API role guard connection plan

## Loop 042: admin API role guard connection plan

### Date
2026-06-14

### What changed
- Admin API role guard connection planをdocs-onlyで追加。
- `apps/api/src/index.ts` の実routeをもとにAdmin API route inventoryを整理。
- routeごとの `AdminAction` mapping、初期許可role、guard対象/対象外を整理。
- dev-only seed、health、LINE webhookのrole guard対象外方針を整理。
- `source: dev_header` / `authenticated_staff`、`permission_denied` response、接続Loop順を整理。
- README、dev loop docs、Loop 042 task docを更新。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- lint / typecheck / test / test:integration 成功。
- 28 files / 184 tests passed。

### Tenant isolation
- runtime routeはまだdev-only `x-tenant-id` pathのまま維持。
- route/action mappingはtenant-scoped Admin routesだけをguard対象にし、LINE webhookは別境界として扱う。

### External API
- Supabase Auth/JWT接続なし。
- Supabase本番接続なし。
- LINE APIなし。
- OpenAI APIなし。

### Risks
- Admin API route guard、Admin UI制御、authenticated runtime接続は未実装。
- dev header production rejectionは別Loop。

### Next
- Loop 043: admin API role guard boundary
