# Development Log: 2026-06-13

## Loop 000: scaffold

### What changed
- Monorepo scaffold、基本docs、AGENTS.md、標準検証コマンドを整備。

### Result
- 初期開発ループの土台を作成。

## Loop 001: database schema

### What changed
- Supabase PostgreSQL想定の初期migration SQL、tenant seed、domain型、validation、DB schema testsを追加。

### Tenant isolation
- 主要テーブルに `tenant_id` を持たせ、初期tenantを `tenant_amamihome` として定義。

## Loop 002: LINE webhook foundation

### What changed
- LINE webhook route、webhookSecretからのtenant解決、raw body署名検証、fixture testsを追加。

### External API
- LINE API実送信なし。

## Loop 003: message logging

### What changed
- follow/text message eventからcustomer upsertとmessage loggingを行うdomain/repository土台を追加。

### Tenant isolation
- webhookSecretから解決した `tenant_id` をcustomer/messageへ付与。

## Loop 004: admin customer list

### What changed
- `GET /api/admin/customers` を追加し、dev header `x-tenant-id` でtenant別customer一覧を取得。

### Tenant isolation
- 他tenant customerが混ざらないことをテスト。

## Loop 005: customer timeline

### What changed
- customer detail APIとtimeline APIを追加。

### Tenant isolation
- 別tenant customerは404扱いにし、存在推測を避ける設計にした。

## Loop 006: staff reply

### What changed
- staff reply API、LineClient interface、MockLineClient送信、staff message保存を追加。

### External API
- 実LINE APIは呼ばずmockで検証。

## Loop 007: unreplied alert

### What changed
- 未返信判定service、AlertRepository、`POST /api/admin/alerts/check-unreplied` を追加。

### Tenant isolation
- known tenantのcustomersだけを未返信判定対象にした。

## Loop 008: staff notification

### What changed
- StaffNotifier interface、MockStaffNotifier、open alert通知、notified status更新を追加。

### External API
- 担当者LINE実送信なし。

## Loop 009: AI summary

### What changed
- MockAiProviderによる会話要約APIを追加し、AI summary messageとしてtimelineに保存。

### External API
- OpenAI API実呼び出しなし。

## Loop 010: AI reply draft

### What changed
- AI返信下書きAPIを追加し、draftをAPIレスポンスで返すだけにした。

### Risks
- 下書き保存はDB schema syncが必要なため未実装。

## Loop 011: RAG foundation

### What changed
- KnowledgePageRepository、in-memory RAG search、tenant scoped keyword search APIを追加。

### Tenant isolation
- RAG検索は `tenant_id` で先に絞る。

## Loop 012: Amami Home knowledge import

### What changed
- アマミホーム向け静的knowledge seedを追加し、RAG検索で使える状態にした。

### External API
- 公式HPクロールやWebアクセスなし。

## Loop 013: RAG answer draft

### What changed
- RAG検索結果をsourceにした回答案APIを追加。

### External API
- OpenAI API、LINE API、Supabase接続なし。

## Loop 014: admin read-only UI foundation

### What changed
- Next.js管理画面にトップ、顧客一覧、顧客詳細、timeline read-only UIを追加。

### Result
- `npx pnpm@10.12.1 build` 成功。

## Loop 015: local dev runbook and demo seed

### Date
2026-06-13

### What changed
- 開発用 `POST /api/dev/seed-demo-data` を追加。
- demo顧客・messagesをin-memoryへ投入できるようにした。
- READMEにローカル管理画面確認手順を追記。

### Commands
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- 15 files / 71 tests passed。

### Tenant isolation
- demo seedは `x-tenant-id` 必須。
- customer/messageに `tenant_id = tenant_amamihome` を付与。

### External API
- LINE APIなし。
- OpenAI APIなし。
- Supabase接続なし。

### Risks
- in-memoryなのでAPI process再起動で消える。

### Next
- Loop 016: admin action UI foundation

## Loop 016: admin action UI foundation

### Date
2026-06-13

### What changed
- 顧客詳細画面にAI要約アクションを追加。
- AI返信下書きアクションを追加。
- RAG回答案フォームを追加。
- Server Action経由でadmin server側からAPIをPOSTする構成にした。
- LINE送信UI、staff reply送信UIはまだ追加していない。

### Commands
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`

### Result
- lint / typecheck / test / test:integration / build 成功。
- 15 files / 75 tests passed。

### Tenant isolation
- admin API helperがAI要約、AI返信下書き、RAG回答案POSTに `x-tenant-id` を付与。

### External API
- OpenAI APIなし。
- LINE APIなし。
- Supabase接続なし。

### Risks
- RAG sources表示はAPIレスポンスにsourcesがある場合に表示される。
- LINE送信とstaff reply送信UIは後続Loop。

### Next
- Loop 017: staff reply UI foundation

## Loop 017: staff reply UI foundation

### Date
2026-06-13

### What changed
- 顧客詳細画面に担当者返信フォームを追加。
- Server Action経由で既存のstaff reply APIを呼ぶ構成にした。
- admin API helperに `sendStaffReply` を追加。
- `x-tenant-id` と開発用 `x-staff-id` を付けてPOSTするようにした。
- 成功/失敗を画面に表示し、送信後にtimeline更新を試みるようにした。

### Commands
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`

### Result
- lint / typecheck / test / test:integration / build 成功。

### Tenant isolation
- admin API helperがstaff reply POSTに `x-tenant-id` を付与。

### External API
- 実LINE APIなし。
- OpenAI APIなし。
- Supabase接続なし。

### Risks
- まだ開発用Mock送信であり、本番LINE送信は未接続。
- 本番認証、JWT、RLSは未実装。

### Next
- Loop 018: alert UI foundation

## Loop 018: alert UI foundation

### Date
2026-06-13

### What changed
- `GET /api/admin/alerts` を追加。
- 管理画面に `/alerts` ページを追加。
- 未返信チェック実行ボタンを追加。
- open alert通知mock実行ボタンを追加。
- alert一覧でstatus / severity / type / customer_id / message / notified_at / created_atを確認できるようにした。

### Commands
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`

### Result
- lint / typecheck / test / test:integration / build 成功。

### Tenant isolation
- alert list APIとadmin helperは `x-tenant-id` 必須。
- 他tenant alertは一覧に返さない。

### External API
- 実LINE APIなし。
- OpenAI APIなし。
- Supabase接続なし。

### Risks
- 通知はMockStaffNotifier前提で、本番LINEグループ通知は未接続。
- alert永続化、認証、RLSは未実装。

### Next
- Loop 019: local manual test checklist

## Loop 019: local manual test checklist

### Date
2026-06-13

### What changed
- ローカルMVP手動確認用runbookを追加。
- API/Admin起動、demo seed、顧客確認、AI/RAG、担当者返信、alert確認の手順を整理。
- READMEからrunbookへリンク。
- dev loop docsに主要UI loop後はmanual checklistを更新する方針を追記。

### Commands
- `git diff --check`
- `npx pnpm@10.12.1 lint`

### Result
- docs-only変更として検証成功。

### Tenant isolation
- runbookでは `TENANT_ID = tenant_amamihome` と `x-tenant-id: tenant_amamihome` を前提にした。

### External API
- 本物のLINE API、OpenAI API、Supabaseには接続しない手順として明記。

### Risks
- 手動確認はin-memory前提なのでAPI process再起動でdemo dataが消える。

### Next
- Loop 020: Supabase persistence planning
