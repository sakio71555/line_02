# Development Log: 2026-06-26

## Loop 114: Nginx actual enable + reload rollbackable dry-run

### Date
2026-06-26

### What changed
- `amami-line-crm.invalid` のまま、一時 `sites-enabled` symlink作成、`nginx -t`、Nginx reload、Host header smokeを実施した。
- Host header smokeで `/api/health` が `404` となったためNo-Goとして記録した。
- 一時symlinkを削除し、削除後の `nginx -t` とrollback reloadを実行して未公開状態へ戻した。
- Loop 114 task doc、runbook、production readiness、README、dev loop docsへ結果を追記した。

### Commands
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`
- VPS preflight via SSH
- temporary `sites-enabled` symlink + `sudo nginx -t`
- `sudo systemctl reload nginx`
- localhost + `Host: amami-line-crm.invalid` smoke
- symlink removal + rollback `sudo nginx -t` + reload

### Result
- Local test / integration: 99 files passed / 1 skipped、655 passed / 4 skipped。
- Local build: 10 packages successful。
- VPS candidate `/etc/nginx/sites-available/amami-line-crm.conf` was present。
- temporary symlink was created and `sudo nginx -t` passed。
- `sudo systemctl reload nginx` was executed。
- Host header smoke: `/` was `200`, `/api/health` was `404`。
- temporary symlink was removed。
- rollback `sudo nginx -t` and reload succeeded。
- direct API `/health` and Admin `/login` remained `200` after rollback。
- production readiness remains `production_no_go`。

### Tenant isolation
- runtime/API/DB codeは変更なし。
- `tenant_id` 分離ロジックは未変更。

### External API
- LINE APIなし。
- OpenAI APIなし。
- Supabase connectionなし。
- DNS、HTTPS/certbot、real domain、external smokeなし。

### Risks
- Candidate is reloadable, but Host header routing is not functionally correct yet.
- `/api/health` through Nginx returned `404`; next Loop should diagnose Nginx server selection / route matching.
- production readiness remains `production_no_go`。

### Next
- Loop 115: Nginx Host-header routing diagnosis

## Loop 115: Nginx Host-header routing diagnosis and local stage fix

### Date
2026-06-26

### What changed
- Loop 114 commit `76b6914` を監査し、未push分として `origin/main` へpushした。
- Loop 109-114のObsidian互換dev log、task docs、runbooks、production readinessを棚卸しし、専用 `.obsidian` は作らず `docs/14_dev_logs/` を正とした。
- Nginx candidateへ `X-Amami-Line-Crm-Proxy` diagnostic headerを追加した。
- VPS上でstandalone localhost-only Nginx `127.0.0.1:18080` を使い、Host header routingと `/api/health` mappingを確認した。
- VPS `sites-available` candidateを更新し、一時 `sites-enabled` symlink + `sudo nginx -t` だけを確認した。
- Loop 115 task doc、Nginx diagnosis runbook、production readiness、README、dev loop docsへ結果を追記した。

### Commands
- `git fetch --prune origin`
- `git diff --check origin/main..HEAD`
- `git push origin HEAD:main`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`
- VPS direct Admin/API smoke via SSH
- VPS standalone localhost-only Nginx smoke on `127.0.0.1:18080`
- VPS candidate update
- temporary `sites-enabled` symlink + `sudo nginx -t`

### Result
- Loop 114 commit `76b6914` pushed successfully.
- `git diff --check`: success.
- `npx pnpm@10.12.1 lint`: success.
- `npx pnpm@10.12.1 typecheck`: success.
- `npx pnpm@10.12.1 test`: 100 files passed / 1 skipped、658 tests passed / 4 skipped.
- `npx pnpm@10.12.1 test:integration`: 100 files passed / 1 skipped、658 tests passed / 4 skipped.
- `npx pnpm@10.12.1 build`: 10 packages successful.
- direct API `/health` returned `200`; direct API `/api/health` returned `404`, matching the API contract.
- standalone Nginx returned `200` for `/`, `/login`, `/select-tenant`, `/customers`, `/alerts`, `/permission-denied`, `/session-expired`, and `/api/health`.
- `/api/health` returned `X-Amami-Line-Crm-Proxy: amami-line-crm`.
- static asset smoke returned `308`, showing the request reached the Admin app path and was redirected by app/static route handling.
- candidate backup created at `/etc/nginx/sites-available/amami-line-crm.conf.loop115-backup-20260626-135905`.
- temporary symlink was removed and final `sudo nginx -t` passed.
- `127.0.0.1:18080` was absent after cleanup.
- system Nginx reload/restart was not executed.
- production readiness remains `production_no_go`.

### Tenant isolation
- runtime/API/DB codeは変更なし。
- `tenant_id` 分離ロジックは未変更。

### External API
- LINE APIなし。
- OpenAI APIなし。
- Supabase connectionなし。
- real domain、DNS、HTTPS/certbot、external smokeなし。

### Risks
- Loop 115ではsystem Nginx reload/restartを実行していないため、diagnostic header付きのactive Host header smokeは未確認。
- real domain、DNS、HTTPS、public external smokeは未実施。
- production readiness remains `production_no_go`。

### Next
- Loop 116: Domain/DNS/HTTPS readiness checklist

## Loop 116: Domain / DNS / HTTPS readiness inventory

### Date
2026-06-26

### What changed
- 実ドメイン、DNS、HTTPS公開前のreadiness inventoryを追加した。
- canonical hostname、DNS provider、domain ownership、ACME method、certificate SAN、LINE webhook public URLを未決定事項として整理した。
- placeholder-basedのHTTP bootstrap / HTTPS Nginx exampleを追加した。
- read-only preflight helperを追加した。
- Loop 116 task doc、domain/DNS/HTTPS runbook、production readiness、README、dev loop docsへ結果を追記した。

### Commands
- `git status --short`
- `git status --short --branch`
- `git log --oneline -5`
- `git fetch --prune origin`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`
- VPS read-only inventory via SSH

### Result
- `git diff --check`: success.
- `npx pnpm@10.12.1 lint`: success.
- `npx pnpm@10.12.1 typecheck`: 10 packages successful.
- `npx pnpm@10.12.1 test`: 101 files passed / 1 skipped、662 tests passed / 4 skipped.
- `npx pnpm@10.12.1 test:integration`: 101 files passed / 1 skipped、662 tests passed / 4 skipped.
- `npx pnpm@10.12.1 build`: 10 packages successful.
- VPS read-only inventory showed Nginx active, `nginx -t` success, public 80/443 listeners, app localhost listeners on `127.0.0.1:3002` / `127.0.0.1:8788`, certbot installed, and no enabled amami-line-crm include.
- DNS query was not run because canonical hostname is not approved.
- Nginx active config変更、reload/restart、certbot、DNS変更、external smokeは未実施。
- production readiness remains `production_no_go`.

### Tenant isolation
- runtime/API/DB codeは変更なし。
- `tenant_id` 分離ロジックは未変更。

### External API
- LINE APIなし。
- OpenAI APIなし。
- Supabase connectionなし。
- DNS変更、HTTPS/certbot、real domain、external smokeなし。

### Risks
- canonical hostname、DNS provider、domain ownership、ACME method、certificate SAN、LINE webhook public URLが未決定。
- selected tenant cookieのHTTPS production属性とCORS方針は、実ドメイン決定後に再確認が必要。
- production readiness remains `production_no_go`.

### Next
- Loop 117: real domain decision and DNS provider confirmation plan

## Loop 117: Real domain decision and DNS provider confirmation plan

### Date
2026-06-26

### What changed
- real domain公開前のdomain decision packet、DNS provider confirmation checklist、production domain approval sheetを追加した。
- 候補ドメインを分類し、canonical hostname、DNS provider、domain owner、DNS rollback owner、LINE webhook public URLは `unknown` のまま維持した。
- read-only preflight helperへ、canonical hostname未決定時にDNS queryをskipする `--no-dns` modeを追加した。
- Loop 117 task doc、README、dev loop docs、domain/DNS/HTTPS runbook、production readinessへNo-Go状態を追記した。

### Commands
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`

### Result
- `git diff --check`: success.
- `npx pnpm@10.12.1 lint`: success.
- `npx pnpm@10.12.1 typecheck`: 10 packages successful.
- `npx pnpm@10.12.1 test`: 102 files passed / 1 skipped、668 tests passed / 4 skipped.
- `npx pnpm@10.12.1 test:integration`: 102 files passed / 1 skipped、668 tests passed / 4 skipped.
- `npx pnpm@10.12.1 build`: 10 packages successful.
- DNS query was skipped because no canonical hostname is approved.
- Nginx active config変更、reload/restart、certbot、DNS変更、external smoke、LINE webhook登録は未実施。
- production readiness remains `production_no_go`.

### Tenant isolation
- runtime/API/DB codeは変更なし。
- `tenant_id` 分離ロジックは未変更。

### External API
- LINE APIなし。
- OpenAI APIなし。
- Supabase connectionなし。
- DNS provider APIなし。
- DNS queryなし。

### Risks
- canonical hostname、DNS provider、domain owner、DNS rollback owner、ACME method、certificate names、LINE webhook public URLが未承認。
- production readiness remains `production_no_go`.

### Next
- Loop 118: approved domain read-only DNS confirmation

## Loop 118: Approved domain read-only DNS confirmation

### Date
2026-06-26

### What changed
- `admin.taiyolabel.site` を検証/管理用ホストとして、TXTを除くread-only DNS確認結果を記録した。
- `admin.taiyolabel.site A 160.251.174.201` が期待IPと一致することを確認した。
- NSからDNS providerを `dnsv.jp / GMO DNS` と推定したが、DNS owner / rollback ownerは未確定のまま残した。
- VPS read-only確認で `nginx -t` 成功、`sites-enabled` candidateなし、localhost API/Admin 200を記録した。
- Loop 118 task doc、DNS inventory runbook、approval docs、production readiness、README、dev loop docsを更新した。

### Commands
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`
- read-only `dig` queries for NS/SOA/MX/CAA/DS/A/AAAA/CNAME
- VPS read-only SSH checks

### Result
- Pre-DNS quality gate passed.
- DNS A record matched the expected VPS IP.
- TXT records were not fetched.
- DNS changes were not made.
- Nginx reload/restart, certbot, external HTTP/HTTPS smoke were not run.
- production readiness remains `production_no_go`.

### Tenant isolation
- runtime/API/DB codeは変更なし。
- `tenant_id` 分離ロジックは未変更。

### External API
- LINE APIなし。
- OpenAI APIなし。
- Supabase connectionなし。
- DNS provider APIなし。

### Risks
- DNS owner、DNS rollback owner、Nginx enable approver、Certificate approver、LINE webhook approver、Maintenance windowは未確定。
- ACME method is undecided.
- A record一致はpublic enablement承認ではない。

### Next
- Loop 119: domain owner and rollback owner approval record

## Loop 119: Domain owner and rollback owner approval record

### Date
2026-06-26

### What changed
- domain and release approval recordを追加し、Domain owner / DNS change owner / DNS rollback owner / Nginx enable approver / Certificate approver / LINE webhook approver / External smoke approver / Maintenance window / Final Go-No-Go ownerを `unknown` / `pending` として記録した。
- DNS / Nginx rollback owner checklistを追加し、DNS rollback、Nginx rollback、certificate rollback、application rollback、rollback triggers、No-Go条件を整理した。
- `admin.taiyolabel.site` はreview/admin hostnameとして記録し、client-facing final hostnameは `undecided` のまま維持した。
- production readiness、approval sheet、domain/DNS runbooks、README、dev loop docsを更新した。

### Commands
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`

### Result
- Approval owner record and rollback checklist were added.
- DNS changes were not made.
- TXT records were not fetched.
- Nginx reload/restart was not run.
- certbot / HTTPS was not run.
- external HTTP/HTTPS smoke was not run.
- production readiness remains `production_no_go`.

### Tenant isolation
- runtime/API/DB codeは変更なし。
- `tenant_id` 分離ロジックは未変更。

### External API
- LINE APIなし。
- OpenAI APIなし。
- Supabase connectionなし。
- DNS provider APIなし。

### Risks
- All required human owners / approvers remain unknown or pending.
- client-facing final hostname is undecided.
- ACME method is undecided.

### Next
- Loop 120: release commit alignment and VPS reproducible redeploy
