# Dev Log: 2026-07-03

## Loop 313: LIFF customer registration and contact change

- Added a production-oriented LIFF customer registration/contact-change form at `/line/customer-registration`.
- Added LIFF ID token verification on the API before CRM customer profile writes.
- Added `/api/liff/customer-profile` to upsert CRM customer detail and save a sanitized form summary to the timeline.
- Added `/api/liff/runtime-config` so the LIFF form can load the public LIFF ID from runtime config.
- Updated customer detail to show registration status and postal code.
- Updated rich menu definitions so initial customer registration and aftercare contact change open the LIFF form URLs.
- Added integration tests for LIFF customer profile writes, ID token verifier behavior, and updated rich menu action counts.
- LINE real send, OpenAI API execution, Supabase direct connection, DB migration, LINE rich menu API apply, and secret/raw log recording were not performed during implementation.
- Production Go remains `true`; DR readiness remains `not_ready_restore_failed`.

## Loop 314: customer account and staff notification account boundary

- Corrected the LINE account boundary after operator clarification.
- Existing customer-facing LINE Official Account remains customer-facing and keeps the customer rich menu.
- Newly created internal LINE Official Account is reserved for staff notification only when customer LINE updates or inquiries arrive.
- Updated staff notification payloads to send brief Japanese summaries only, with details kept in Admin.
- Avoided copying raw alert messages into staff notification text.
- Added a safe `--remove-default` rich menu operator command for the separate internal notification account only.
- Restored the customer-facing rich menu after the accidental default removal.
- LINE message send, OpenAI API execution, Supabase direct connection, DB migration, Nginx/DNS/HTTPS/certbot changes, and secret/raw log recording were not performed.
- Production Go remains `true`; DR readiness remains `not_ready_restore_failed`.

## Loop 315: customer LINE update staff alert entrypoints

- Added a domain helper to create one open unresolved alert for a customer when no active unresolved alert already exists.
- LINE webhook text messages now mark the customer as `human_required` and create an unresolved alert for staff follow-up.
- LIFF customer registration/contact-change submissions now create an unresolved alert after CRM customer detail and timeline writes.
- Staff notification remains a separate later delivery step through the `アマミホーム 相談受付` account; this Loop only creates the CRM alert target.
- LINE message send, OpenAI API execution, Supabase direct connection, DB migration, Nginx/DNS/HTTPS/certbot changes, VPS runtime changes, and secret/raw log recording were not performed.
- Production Go remains `true`; DR readiness remains `not_ready_restore_failed`.
