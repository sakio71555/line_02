# Development Log: 2026-06-22

## Loop 111: VPS localhost-only redeploy and mobile UI review

### Date
2026-06-22

### What changed
- VPS localhost-only review環境へAdmin mobile UIを再配置した。
- VPS releaseはgit worktreeではなく `DEPLOYED_COMMIT` を使うcopy-based releaseとして確認した。
- Browser smokeで見つかったページヘッダー戻るリンクのtap targetを44px以上へ最小修正した。
- Loop 111 task doc、VPS localhost runbook、mobile review checklist、production readiness docsへ証跡を追記した。

### Commands
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`
- VPS localhost-only smoke via SSH
- Browser mobile smoke via SSH tunnel

### Result
- Local test / integration: 97 files passed / 1 skipped、647 passed / 4 skipped。
- Local build: 10 packages successful。
- VPS final deployed source: `176cb34fc6059ecabfb9826daacaabc2a437bebe`。
- API `127.0.0.1:8788` and Admin `127.0.0.1:3002` active。
- Browser smoke: 40 checks completed、horizontal overflow 0、fatal page 0。

### Tenant isolation
- Review runtime remains `REPOSITORY_RUNTIME=in_memory`。
- demo seed and Admin smoke use `tenant_amamihome` only。
- API/Auth/RLS/tenant context code was not changed in this Loop。

### External API
- LINE APIなし。
- OpenAI APIなし。
- Supabase connectionなし。
- Nginx/HTTPS/DNS/public port変更なし。

### Risks
- `/permission-denied` などの準備画面リンクが一部説明文内でinline technical linkとして残る。
- 実機スマホのソフトウェアキーボード挙動は未確認。
- production readinessは引き続き `production_no_go`。

### Next
- Loop 112: Nginx公開前 reverse proxy dry-run plan

## Loop 112: Nginx reverse proxy dry-run plan

### Date
2026-06-22

### What changed
- Nginx公開前のreverse proxy dry-run planをtask docとrunbookへ追加した。
- `_CHANGE_ME_` placeholder付きのrepo-local Nginx exampleを追加した。
- Host header smoke、Go/No-Go、rollback、secret非表示ルールを整理した。
- 追加確認として、VPSの `/etc/nginx/sites-available/amami-line-crm.conf` にcandidateを配置し、`sudo nginx -t` のみ成功確認した。
- production readiness docsとREADME/dev loop docsへ、Nginx公開はまだ未実施であることを追記した。

### Commands
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`
- VPS read-only status check
- VPS sites-available candidate placement
- `sudo nginx -t`

### Result
- VPS deployed commit remains `176cb34fc6059ecabfb9826daacaabc2a437bebe`。
- API `/health` は `200`、Admin `/login` は `200`。
- review ports `3002` / `8788` はpublic bindなし。
- `/etc/nginx/sites-available/amami-line-crm.conf` をcandidateとして作成した。既存同名ファイルはなく、backupはnone。
- candidate server_nameは `amami-line-crm.invalid`、Admin upstreamは `127.0.0.1:3002`、API upstreamは `127.0.0.1:8788`。
- `sudo nginx -t` は成功。
- `/etc/nginx/sites-enabled/amami-line-crm` と `/etc/nginx/sites-enabled/amami-line-crm.conf` は未作成のまま。
- candidateは `nginx -T` のactive configには出ておらず、まだ有効化されていない。
- Nginx有効化、reload/restart、certbot、DNS変更、public公開は未実施。
- production readiness remains `production_no_go`。

### Tenant isolation
- runtime/API/DB codeは変更なし。
- `tenant_id` 分離ロジックは未変更。

### External API
- LINE APIなし。
- OpenAI APIなし。
- Supabase connectionなし。
- Nginxはcandidate配置と `sudo nginx -t` のみ。reload/restartは未実行。

### Risks
- candidateは `sites-available` にあるだけで、active configとしてincludeされていない。
- public enablementには別Loopでhost確定、`sites-enabled` staging、config test、rollback確認、external smokeが必要。

### Next
- Loop 113: Nginx sites-enabled enable dry-run final gate

## Loop 113: Nginx sites-enabled include dry-run final gate

### Date
2026-06-22

### What changed
- VPSで `/etc/nginx/sites-enabled/amami-line-crm.conf` を一時symlinkとして作成し、candidateをNginx include treeへ入れた状態で `sudo nginx -t` を確認した。
- `nginx -T` 要約で `amami-line-crm.invalid`、API upstream `127.0.0.1:8788`、Admin upstream `127.0.0.1:3002` がincludeされることを確認した。
- 確認後、一時symlinkを削除し、削除後の `sudo nginx -t` も成功確認した。
- Loop 113 task doc、runbook、production readiness docs、README/dev loop docsへ証跡を追記した。

### Commands
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`
- VPS read-only status check
- temporary `sites-enabled` symlink dry-run
- `sudo nginx -t`
- post-cleanup VPS status check

### Result
- local test / integration: 98 files passed / 1 skipped、651 passed / 4 skipped。
- local build: 10 packages successful。
- VPS deployed commit remains `176cb34fc6059ecabfb9826daacaabc2a437bebe`。
- candidate `/etc/nginx/sites-available/amami-line-crm.conf` was present。
- temporary symlink was created, `sudo nginx -t` passed, then symlink was removed。
- `/etc/nginx/sites-enabled/amami-line-crm.conf` and `/etc/nginx/sites-enabled/amami-line-crm` are absent after cleanup。
- post-cleanup `sudo nginx -t` passed。
- API `/health` は `200`、Admin `/login` は `200`。
- review ports `3002` / `8788` はlocalhost-only維持。
- Nginx reload/restart、certbot、DNS変更、public公開は未実施。
- production readiness remains `production_no_go`。

### Tenant isolation
- runtime/API/DB codeは変更なし。
- `tenant_id` 分離ロジックは未変更。

### External API
- LINE APIなし。
- OpenAI APIなし。
- Supabase connectionなし。
- Nginxは一時include dry-runと `sudo nginx -t` のみ。reload/restartは未実行。

### Risks
- candidateはinclude時のsyntax確認済みだが、現在はsymlink削除済みで未公開。
- actual enableには、実host確定、reload approval、rollback確認、external smoke、HTTPS/DNS判断がまだ必要。

### Next
- Loop 114: actual Nginx enable + reload final approval gate
