# Development Log: 2026-06-22

## Loop 111: VPS localhost-only redeploy and mobile UI review

### Date
2026-06-22

### What changed
- VPS localhost-only review環境へAdmin mobile UIを再配置した。
- VPS releaseはgit worktreeではなく `DEPLOYED_COMMIT` を使うcopy-based releaseとして確認した。
- Browser smokeで見つかったページヘッダー戻るリンクのtap targetを44px以上へ最小修正した。
- Loop 111 task doc、VPS localhost runbook、mobile review checklist、production readiness docsへ証跡を追記した。

### Commands
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`
- VPS localhost-only smoke via SSH
- Browser mobile smoke via SSH tunnel

### Result
- Local test / integration: 97 files passed / 1 skipped、647 passed / 4 skipped。
- Local build: 10 packages successful。
- VPS final deployed source: `176cb34fc6059ecabfb9826daacaabc2a437bebe`。
- API `127.0.0.1:8788` and Admin `127.0.0.1:3002` active。
- Browser smoke: 40 checks completed、horizontal overflow 0、fatal page 0。

### Tenant isolation
- Review runtime remains `REPOSITORY_RUNTIME=in_memory`。
- demo seed and Admin smoke use `tenant_amamihome` only。
- API/Auth/RLS/tenant context code was not changed in this Loop。

### External API
- LINE APIなし。
- OpenAI APIなし。
- Supabase connectionなし。
- Nginx/HTTPS/DNS/public port変更なし。

### Risks
- `/permission-denied` などの準備画面リンクが一部説明文内でinline technical linkとして残る。
- 実機スマホのソフトウェアキーボード挙動は未確認。
- production readinessは引き続き `production_no_go`。

### Next
- Loop 112: Nginx公開前 reverse proxy dry-run plan

## Loop 112: Nginx reverse proxy dry-run plan

### Date
2026-06-22

### What changed
- Nginx公開前のreverse proxy dry-run planをtask docとrunbookへ追加した。
- `_CHANGE_ME_` placeholder付きのrepo-local Nginx exampleを追加した。
- Host header smoke、Go/No-Go、rollback、secret非表示ルールを整理した。
- production readiness docsとREADME/dev loop docsへ、Nginx公開はまだ未実施であることを追記した。

### Commands
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`
- VPS read-only status check

### Result
- VPS deployed commit remains `176cb34fc6059ecabfb9826daacaabc2a437bebe`。
- API `/health` は `200`、Admin `/login` は `200`。
- review ports `3002` / `8788` はpublic bindなし。
- `/etc/nginx/sites-enabled/amami-line-crm` と `/etc/nginx/sites-available/amami-line-crm` は未作成。
- Nginx config enable、reload/restart、certbot、DNS変更、public公開は未実施。
- production readiness remains `production_no_go`。

### Tenant isolation
- runtime/API/DB codeは変更なし。
- `tenant_id` 分離ロジックは未変更。

### External API
- LINE APIなし。
- OpenAI APIなし。
- Supabase connectionなし。
- Nginxはread-only確認のみ。

### Risks
- reverse proxy configはまだVPSへstaged testしていない。
- public enablementには別Loopでhost確定、config test、rollback確認、external smokeが必要。

### Next
- Loop 113: Nginx reverse proxy staged config test
