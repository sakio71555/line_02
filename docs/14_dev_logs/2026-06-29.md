# Development Log 2026-06-29

## Loop 202: pg_dump 17 client boundary and backup mismatch runbook

### Date

2026-06-29

### What changed

- Recorded the post-Loop 201 backup export failure category as `pg_dump_server_version_mismatch`.
- Recorded sanitized versions: `detected_server_major_or_version=17.6` and `detected_pg_dump_major_or_version=16.14`.
- Documented that backup export must not be retried with `pg_dump` 16.x.
- Added a runbook and static tests for the PostgreSQL 17 client boundary.

### Commands

- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`

### Result

- `pg_dump_failure_categories=pg_dump_server_version_mismatch`
- `detected_server_major_or_version=17.6`
- `detected_pg_dump_major_or_version=16.14`
- `raw_log_not_displayed=true`
- `secrets_recorded=false`
- `pg_dump_17_client_required=true`
- `pg_dump_16_retry_allowed=false`
- `backup_export_retry_before_pg_dump_17=false`
- `DB export performed=false`
- `backup artifact created=false`
- `restore performed=false`
- `production_restore_performed=false`

### Tenant isolation

- No tenant data was exported.
- No database connection was made in this Loop.
- No API, repository, RLS, tenant routing, migration, schema, or Supabase write changes were made.

### External API

- Supabase CLI/API was not called.
- `pg_dump` was not re-executed.
- Database export was not performed.
- Restore was not performed.
- Backup artifact was not created, downloaded, uploaded, or deleted.
- Additional LINE send was not performed.
- OpenAI API was not called.
- Nginx reload/restart, DNS, HTTPS, certbot, and public smoke changes were not performed.
- Secrets and raw logs were not recorded.

### Risks

- Supabase backup success is still not achieved.
- A PostgreSQL 17-compatible `pg_dump` client is required before another controlled export attempt.
- PostgreSQL client 17 installation must be a separate approved Loop.

### Next

- Loop 203: PostgreSQL 17 client installation preflight

## Loop 202.1: Supabase DB URL secret replacement

### Date

2026-06-29

### What changed

- Replaced the incorrect `SUPABASE_DB_URL` through operator input and root-only handling.
- Recorded only sanitized boolean evidence.
- Kept Loop 202 backup export boundary in place.

### Commands

- `git status --short`
- VPS boolean check only
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`

### Result

- `secret_replaced=true`
- `present=true`
- `format_check=passed`
- `secrets_recorded=false`
- `raw_log_not_displayed=true`
- `pg_dump reexecuted=false`
- `DB export performed=false`
- `backup artifact created=false`
- `restore performed=false`
- `production_restore_performed=false`

### Tenant isolation

- No tenant data was exported or inspected.
- No database query, repository change, migration, RLS change, or runtime route change was performed.

### External API

- Supabase API/CLI was not called.
- `pg_dump` was not re-executed.
- LINE send, OpenAI API call, Nginx reload/restart, DNS, HTTPS, certbot, and public smoke were not performed.
- Secret values, DB URL, `.env` contents, and raw logs were not recorded.

### Risks

- Supabase backup success is still not achieved.
- PostgreSQL client 17 is still required before backup export can be retried.

### Next

- Loop 203: PostgreSQL 17 client installation preflight

## Loop 203: PostgreSQL 17 client installation preflight

### Date

2026-06-29

### What changed

- Checked VPS OS/package state before PostgreSQL 17 client installation.
- Confirmed current PostgreSQL client is still major 16 and backup export remains blocked by the Loop 202 mismatch boundary.
- Documented PostgreSQL 17 client installation candidates, explicit-path policy, rollback boundary, and Loop 204 Go/No-Go.
- Updated task doc, pg_dump boundary runbook, and Obsidian-facing log.

### Commands

- `pwd`
- `git status -sb`
- `git log --oneline -5`
- VPS read-only OS/package checks
- `git status --short`
- `git diff --check`
- secret literal check
- `npx pnpm@10.12.1 lint`

### Result

- `vps_preflight_status=completed_read_only`
- `os_id=ubuntu`
- `os_version=24.04.3_LTS`
- `os_codename=noble`
- `pg_dump_available=true`
- `current_pg_dump_path=/usr/bin/pg_dump`
- `current_pg_dump_wrapper=/usr/share/postgresql-common/pg_wrapper`
- `current_pg_dump_binary=/usr/lib/postgresql/16/bin/pg_dump`
- `current_pg_dump_version_source=postgresql-client-16_package`
- `current_pg_dump_version=16.14-0ubuntu0.24.04.1`
- `current_pg_dump_major=16`
- `detected_server_major_or_version=17.6`
- `postgresql_apt_source_detected=false`
- `postgresql_17_client_candidate_available=false_current_apt_cache`
- `pg_dump_17_binary_candidate_path=/usr/lib/postgresql/17/bin/pg_dump`
- `package_install_executed=false`
- `apt_update_executed=false`
- `apt_install_executed=false`
- `pg_dump_executed=false`
- `supabase_connection_executed=false`
- `db_export_executed=false`
- `backup_artifact_created=false`
- `restore_executed=false`
- `secrets_recorded=false`
- `raw_log_displayed=false`

### Tenant isolation

- No tenant data was exported or inspected.
- No database query, repository change, migration, RLS change, or runtime route change was performed.

### External API

- Supabase API/CLI was not called.
- `pg_dump` and `psql` were not executed.
- LINE send, OpenAI API call, Nginx reload/restart, DNS, HTTPS, certbot, and public smoke were not performed.
- Secret values, DB URL, `.env` contents, secret file contents, and raw logs were not recorded.

### Risks

- Supabase backup success is still not achieved.
- PostgreSQL 17 client is not installed.
- Current Ubuntu APT cache did not expose `postgresql-client-17`; PostgreSQL official APT repository approval may be required.
- Incorrect binary path usage could reproduce the version mismatch.

### Next

- Loop 204: PostgreSQL 17 client installation approval and execution

## Loop 204: PostgreSQL 17 client installation approval and execution

### Date

2026-06-29

### What changed

- Added PGDG APT source and key on the VPS for PostgreSQL client packages.
- Ran `apt update`.
- Installed `postgresql-client-17`.
- Verified `/usr/lib/postgresql/17/bin/pg_dump --version` reports PostgreSQL 17.10.
- Confirmed existing `/usr/lib/postgresql/16/bin/pg_dump` remains PostgreSQL 16.14.
- Documented rollback boundary, package changes, and Loop 205 Go/No-Go.

### Commands

- `pwd`
- `git status -sb`
- `git log --oneline -5`
- VPS install-pre inventory
- PGDG source/key addition
- `apt-get update`
- `apt-get install -y --no-install-recommends postgresql-client-17`
- explicit-path `pg_dump --version` checks
- `git status --short`
- `git diff --check`
- secret literal check
- `npx pnpm@10.12.1 lint`

### Result

- `pgdg_source_added=true`
- `pgdg_source_file=/etc/apt/sources.list.d/pgdg.list`
- `pgdg_key_file=/usr/share/postgresql-common/pgdg/apt.postgresql.org.asc`
- `apt_update_executed=true`
- `apt_install_executed=true`
- `postgresql_client_17_installed=true`
- `installed_package=postgresql-client-17 17.10-1.pgdg24.04+1`
- `dependency_upgraded=libpq5 18.4-1.pgdg24.04+1`
- `postgresql_server_17_installed=false`
- `pg_dump_17_path=/usr/lib/postgresql/17/bin/pg_dump`
- `pg_dump_17_version=17.10`
- `pg_dump_17_version_check_passed=true`
- `pg_dump_16_preserved=true`
- `usr_bin_pg_dump_symlink_modified=false`
- `pg_wrapper_package_modified=false`
- `pg_dump_update_alternatives_modified=false`
- `pg_dump_db_connection_executed=false`
- `psql_executed=false`
- `supabase_connection_executed=false`
- `db_export_executed=false`
- `backup_artifact_created=false`
- `restore_executed=false`
- `secrets_recorded=false`
- `raw_log_displayed=false`
- `loop_205_backup_export_retry_ready=true_after_operator_approval`

### Tenant isolation

- No tenant data was exported or inspected.
- No database query, repository change, migration, RLS change, or runtime route change was performed.

### External API

- Supabase API/CLI was not called.
- `pg_dump` was used only for local `--version` checks, not DB connection or export.
- `psql` was not executed.
- LINE send, OpenAI API call, Nginx reload/restart, DNS, HTTPS, certbot, and public smoke were not performed.
- Secret values, DB URL, `.env` contents, secret file contents, and raw logs were not recorded.

### Risks

- Backup export is still not proven successful.
- Bare `pg_dump --version` now resolves to 17.10 through pg_wrapper; future export must use `/usr/lib/postgresql/17/bin/pg_dump` explicitly.
- PGDG source and `libpq5` dependency upgrade are now part of VPS package state and must be considered in rollback planning.

### Next

- Loop 205: Supabase backup export retry with explicit pg_dump 17 path
