# Development Log: 2026-06-15

## Loop 049: authenticated runtime connection plan

### Date
2026-06-15

### What changed
- `dev_header` runtimeから `authenticated_staff` runtimeへ移行する接続計画をdocs-onlyで追加。
- 現在の `x-tenant-id` / `AdminTenantContext(source: dev_header)` / role guard compatibility skipを整理。
- JWT/session extraction、StaffAuthLookup runtime injection、selectedTenantId、runtime mode、production dev_header rejection方針を整理。
- Admin API role guard、Admin UI placeholder/role visibility fixtures、RLS planとの関係を整理。
- README、database docs、dev loop docs、Loop 049 task docを更新。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- lint / typecheck / test / test:integration 成功。
- 33 files / 216 tests passed。

### Tenant isolation
- `selectedTenantId` はpermissionではなくselectorとして扱い、active membershipで再検証する方針を明記。
- production tenant contextは `staff_tenant_memberships` 由来にする方針を整理。

### External API
- Supabase Auth/JWT接続なし。
- Supabase本番接続なし。
- LINE APIなし。
- OpenAI APIなし。

### Risks
- 現runtimeはまだ `dev_header`。
- JWT/session extraction、StaffAuthLookup runtime injection、Admin API runtime差し替え、RLS SQLは未実装。

### Next
- Loop 050: dev header production rejection plan

## Loop 050: dev header production rejection plan

### Date
2026-06-15

### What changed
- `x-tenant-id` / `dev_header` をproductionで拒否するためのdocs-only計画を追加。
- 現在のAdmin UI helper、Admin API tenant guard、role guard compatibility skip、dev seed routeでのdev_header利用を整理。
- productionで拒否が必要な理由、まだ拒否してはいけない理由、runtime mode matrixを整理。
- environment detection、rejection error response、staging migration、local/test互換、Admin UI/RLSとの関係を整理。
- README、database docs、dev loop docs、Loop 050 task docを更新。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- lint / typecheck / test / test:integration 成功。
- 33 files / 216 tests passed。

### Tenant isolation
- productionではtenant境界を `x-tenant-id` ではなく認証済みstaff membershipから決定する方針を明記。
- local/testのdev_header互換はMVP確認用に限定する方針を整理。

### External API
- Supabase Auth/JWT接続なし。
- Supabase本番接続なし。
- LINE APIなし。
- OpenAI APIなし。

### Risks
- 現runtimeはまだ `dev_header`。
- production rejection、JWT/session extraction、Admin API runtime差し替え、RLS SQLは未実装。

### Next
- Loop 051: Supabase Auth session extraction boundary

## Loop 051: Supabase Auth session extraction boundary

### Date
2026-06-15

### What changed
- Admin API向けに `Authorization: Bearer <token>` を解析するserver-side session extraction boundaryを追加。
- `AuthSessionVerifier` interfaceとfake verifier testを追加し、valid / invalid / expired / missing tokenのerror codeを整理。
- 既存Admin auth error mapperへ接続しやすい `mapAuthSessionErrorToAdminAuthError` を追加。
- README、dev loop docs、Loop 051 task docを更新。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- lint / typecheck / test / test:integration 成功。

### Tenant isolation
- 今回はsession extraction境界のみで、tenant context guardやroute接続は変更なし。
- 後続Loopで `AuthUserIdentity -> StaffAuthLookup -> authenticated_staff tenant context` へ接続する前提を維持。

### External API
- Supabase Auth実接続なし。
- Supabase本番接続なし。
- LINE APIなし。
- OpenAI APIなし。

### Risks
- Admin API routeはまだ `dev_header` runtimeのまま。
- real Supabase Auth verifier、Admin UI token forwarding、production `dev_header` rejectionは未実装。

### Next
- Loop 052: fake authenticated staff runtime connection

## Loop 052: fake authenticated staff runtime connection

### Date
2026-06-15

### What changed
- Admin API向けにfake authenticated runtime boundaryを追加。
- Loop 051のsession extraction boundary、fake `AuthSessionVerifier`、fake `StaffAuthLookup`、authenticated staff tenant guard、role guardを接続。
- `selectedTenantId` によるmulti-tenant membership選択とrole別 `AdminAction` allow/denyをtest。
- README、dev loop docs、Loop 052 task docを更新。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- lint / typecheck / test / test:integration 成功。

### Tenant isolation
- fake `StaffAuthLookup` のactive membershipから `tenantId` とroleを決定。
- `selectedTenantId` はmembership内でのみ許可し、membership外は拒否。

### External API
- Supabase Auth本接続なし。
- Supabase本番接続なし。
- LINE APIなし。
- OpenAI APIなし。

### Risks
- Admin API routeはまだ `dev_header` runtimeのまま。
- real Supabase Auth verifier、Admin UI token forwarding、production `dev_header` rejectionは未実装。

### Next
- Loop 053: representative Admin API route authenticated runtime wiring

## Loop 053: representative Admin API route authenticated runtime wiring

### Date
2026-06-15

### What changed
- `GET /api/admin/customers` にだけfake authenticated_staff runtime wiringを追加。
- Authorization Bearer fake tokenから `AdminTenantContext(source=authenticated_staff)` を作り、`view_customers` role guardをenforce。
- 既存 `x-tenant-id` / `dev_header` pathは互換維持。
- README、dev loop docs、Loop 053 task docを更新。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- lint / typecheck / test / test:integration 成功。

### Tenant isolation
- authenticated routeはmembership由来のtenant_idでcustomer listを取得。
- test-only `authenticatedSelectedTenantId` はmembership内でのみ許可。

### External API
- Supabase Auth本接続なし。
- Supabase本番接続なし。
- LINE APIなし。
- OpenAI APIなし。

### Risks
- authenticated runtimeは代表route 1本のみ。
- selectedTenantId transport、全route rollout、real Supabase Auth verifier、production `dev_header` rejectionは未実装。

### Next
- Loop 054: Admin API authenticated runtime full route rollout plan

## Loop 054: Admin API authenticated runtime full route rollout plan

### Date
2026-06-15

### What changed
- 代表route `GET /api/admin/customers` の authenticated runtime wiringを全Admin API routeへ広げるためのdocs-only rollout planを追加。
- read-only、AI/RAG draft、side-effect routeのPhase分けとroute/action/role matrixを整理。
- `dev_header` 互換、Authorization Bearer path、selectedTenantId transport、fake/real Auth rollout、error response、test/staging/production方針を整理。
- README、dev loop docs、Loop 054 task docを更新。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- lint / typecheck / test / test:integration 成功。

### Tenant isolation
- `selectedTenantId` はpermissionではなくselectorとして扱い、active membershipで必ず再検証する方針を整理。
- Admin API route rolloutでもtenant contextは認証済みstaff membership由来にする方針を維持。

### External API
- Supabase Auth本接続なし。
- Supabase本番接続なし。
- LINE APIなし。
- OpenAI APIなし。

### Risks
- authenticated runtimeはまだ代表route 1本のみ。
- selectedTenantId production transport、real Supabase Auth verifier、Admin UI token forwarding、production `dev_header` rejectionは未実装。

### Next
- Loop 055: authenticated runtime selectedTenantId transport plan

## Loop 055: authenticated runtime selectedTenantId transport plan

### Date
2026-06-15

### What changed
- `selectedTenantId` をAdmin UIからAdmin APIへ渡すためのdocs-only transport planを追加。
- `selectedTenantId` は権限ではなくselectorであり、active membershipで再検証する方針を整理。
- `x-selected-tenant-id` header、query、cookie、server session、storage、request body、URL/subdomain、test-only injectionを比較。
- `/select-tenant` placeholder、Admin API runtime、error response、security、local/test/staging/production方針を整理。
- README、database docs、dev loop docs、Loop 055 task docを更新。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- lint / typecheck / test / test:integration 成功。

### Tenant isolation
- `selectedTenantId` はrepositoryへ直接渡さず、`StaffAuthLookup` とactive membershipで再検証してから `AdminTenantContext.tenantId` を確定する方針を整理。

### External API
- Supabase Auth本接続なし。
- Supabase本番接続なし。
- LINE APIなし。
- OpenAI APIなし。

### Risks
- selectedTenantId transport実装は未実装。
- `/select-tenant` はまだ保存処理なしのplaceholder。
- production `dev_header` rejection、real Supabase Auth verifier、Admin UI token forwardingは未実装。

### Next
- Loop 056: local demo MVP completion hardening

## Loop 056: local demo MVP completion hardening

### Date
2026-06-15

### What changed
- ローカルデモMVP向けにAdminトップ、顧客一覧、顧客詳細、AI/RAG/担当者返信、alerts画面の説明文を補強。
- mock/in-memory/未接続範囲をUI上で明確化。
- local manual test checklistにAuth placeholder、RoleVisibilityNote、デモ当日の説明順を追記。
- README、dev loop docs、Loop 056 task docを更新。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`

### Result
- lint / typecheck / test / test:integration / build 成功。

### Tenant isolation
- runtimeやtenant resolverは変更なし。
- local demoは引き続きdev-only `x-tenant-id` とin-memory repositoryで確認する。

### External API
- Supabase本番接続なし。
- Supabase Auth本接続なし。
- LINE APIなし。
- OpenAI APIなし。
- Web crawlなし。

### Risks
- demo dataはAPI process再起動で消える。
- Auth placeholder、tenant selection、role visibilityは未接続のまま。
- 本番deployや本番外部API接続は未実装。

### Next
- Loop 057: selectedTenantId transport boundary

## Loop 056.1: local demo manual verification record

### Date
2026-06-15

### What changed
- ローカルデモMVPについて、API/Admin起動、demo seed、主要Admin API、Admin UI routeの確認結果を記録。
- `docs/11_codex_tasks/056_1_local_demo_manual_verification_record.md` を追加。
- local manual test checklistに最終確認記録リンク、人間目視チェック、RAG source 0件の注意点を追記。
- READMEとdev loop docsに確認記録方針を追記。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`

### Result
- API/Admin dev server起動確認。
- demo seed、customers、customer detail/timeline、AI summary、AI reply draft、RAG fallback、staff reply、alerts check/notifyをcurl確認。
- Admin主要routeはHTTP 200と主要文言を確認。

### Tenant isolation
- 確認は `tenant_amamihome` と `x-tenant-id` を使うlocal demo pathに限定。
- tenant resolver、runtime、repository実装は変更なし。

### External API
- Supabase本番接続なし。
- LINE APIなし。
- OpenAI APIなし。
- Web crawlなし。

### Risks
- UI目視は人間確認が必要。
- default local runtimeではRAG sourceが0件で、source付きRAG回答案は未確認。

### Next
- Loop 056.2: local demo RAG knowledge seed verification patch

## Loop 056.2: local demo RAG knowledge seed verification patch

### Date
2026-06-15

### What changed
- `POST /api/dev/seed-demo-data` が初期tenant向けAmami Home静的knowledge fixtureもin-memoryへ投入するようにした。
- seed responseに `knowledge_page_count` を追加。
- demo seed後にRAG search / RAG answer draftがsource付きで返るintegration testを追加。
- runbookにsource付きRAG確認keyword、期待結果、source 0件時の対処を追記。
- READMEとLoop 056.2 task docを更新。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`

### Result
- demo seed後に `オンライン相談` で `knowledge_amamihome_online_consultation` をsourceとしてcurl確認。
- `メンテナンス` では `knowledge_amamihome_warranty_maintenance` と `knowledge_amamihome_after_support` をcurl確認。
- RAG answer draftはsource付きの場合 `MockAiProvider` を使い、sourceなしfallbackも既存仕様として維持。

### Tenant isolation
- knowledge fixtureは `tenant_amamihome` のみ。
- RAG search / answer draftは引き続き `tenant_id` で絞る。

### External API
- OpenAI APIなし。
- LINE APIなし。
- Supabase本番接続なし。
- Web crawlなし。

### Risks
- knowledgeはin-memoryのためAPI process再起動で消える。
- 静的fixtureは公式HP最新情報ではないため、価格、在庫、補助金、保証判断などは断定しない。

### Next
- Loop 057: selectedTenantId transport boundary

## Loop 056.3: beginner-friendly POP admin UI direction

### Date
2026-06-15

### What changed
- 初心者でも分かりやすいPOPなAdmin UIへ進めるためのdocs-only方針を追加。
- `docs/16_design/beginner_friendly_pop_admin_ui.md` を作成し、対象ユーザー、文言置き換え、AI/RAG/LINE/mock/危険操作の表示方針を整理。
- 画面別の改善方針と、後続UI Loopの優先順位を整理。
- READMEとdev loop docsに、今後のUI改善ではdesign guidelineを参照する方針を追記。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- docs-only方針整理。
- UI、API、DB schema、外部接続は変更なし。

### Tenant isolation
- runtime変更なし。
- 後続UI改善でもtenant/利用先境界を初心者向けに表示しつつ、tenant_id分離を曖昧にしない方針を整理。

### External API
- LINE APIなし。
- OpenAI APIなし。
- Supabase接続なし。
- Web crawlなし。

### Risks
- 方針整理のみで、実際のUIはまだPOP化していない。
- 画面別に小さいLoopで実装と目視確認が必要。

### Next
- Loop 057: selectedTenantId transport boundary
- Loop 058: beginner-friendly Admin top polish

## Loop 058: beginner-friendly Admin top polish

### Date
2026-06-15

### What changed
- Adminトップだけを初心者向け・少しPOPなローカルデモ入口へ整理。
- 「顧客一覧を見る」を最重要導線、「未返信アラートを見る」を次の導線として表示。
- デモの流れを5ステップにし、AI/ホームページ回答案、担当者返信、未返信アラートの流れを説明。
- デモ用、一時保存、本番LINE未送信、本番AI未接続をラベル風に表示。
- Auth/tenant/role placeholderは「準備中の画面」として補助導線へ整理。
- Adminトップrender testとLoop 058 task docを追加。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`

### Result
- lint / typecheck / test / test:integration / build 成功。
- 37 files / 251 tests passed。
- build時にNext.js ESLint plugin未検出warningは出るが、buildは成功。
- Adminトップをローカルブラウザで表示し、主要導線と注意カードが見えることを確認。

### Tenant isolation
- tenant resolver、API helper、runtimeは変更なし。
- Adminトップでは利用先IDを表示し、tenant分離の前提を維持。

### External API
- LINE APIなし。
- OpenAI APIなし。
- Supabase接続なし。
- Web crawlなし。

### Risks
- 今回はAdminトップのみで、顧客詳細、顧客一覧、alertsのPOP化は未実施。
- RoleVisibilityNoteにはまだ開発者向け表現が残る。
- 実ブラウザ目視は人間確認が必要。

### Next
- Loop 057: selectedTenantId transport boundary
- Loop 059: customer detail action cards POP UI

## Loop 059: customer detail action cards POP UI

### Date
2026-06-15

### What changed
- 顧客詳細画面のAI/RAG/担当者返信まわりだけを初心者向け操作カードUIへ整理。
- AI要約を「相談内容をまとめる」、AI返信下書きを「返信文の下書きを作る」、RAG回答案を「ホームページ情報から回答案を作る」に言い換え。
- 各カードにデモ用、LINEには送信されない、OpenAI API未接続、Webクロール/embedding/pgvector未接続、タイムライン保存のラベル/説明を追加。
- `customer-actions` 用RoleVisibilityNoteだけを「権限ごとの表示制御は準備中です」という初心者向け文言へ変更。
- 操作カードのrender testとLoop 059 task docを追加。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`

### Result
- lint / typecheck / test / test:integration / build 成功。
- 38 files / 252 tests passed。
- build時にNext.js ESLint plugin未検出warningは出るが、buildは成功。
- 顧客詳細をローカルブラウザで表示し、4つの操作カードと未接続/Mock注意文が見えることを確認。

### Tenant isolation
- tenant resolver、API helper、runtimeは変更なし。
- 顧客詳細では利用先ID表示を維持し、tenant分離の前提を変えていない。

### External API
- LINE APIなし。
- OpenAI APIなし。
- Supabase接続なし。
- Web crawlなし。

### Risks
- 顧客詳細の情報テーブルやタイムライン自体はまだ表形式のまま。
- General/alertsのRoleVisibilityNoteには開発者向け表現が残る。
- 実ブラウザ目視は人間確認が必要。

### Next
- Loop 057: selectedTenantId transport boundary
- Loop 060: alerts page beginner-friendly polish

## Loop 060: alerts page beginner-friendly polish

### Date
2026-06-15

### What changed
- alerts画面だけを初心者向け・少しPOPな未返信アラート確認画面へ整理。
- H1を「対応が必要な相談を確認する」にし、未返信チェック、デモ通知、状態の見方を説明。
- 未返信チェックを「手動チェック / デモ用 / scheduler未接続」として表示。
- open alert通知mockを「開いているアラートをデモ通知する」に言い換え、本物のLINE/Slack/メールには通知しないことを明記。
- statusを `open = 対応待ち`、`notified = デモ通知済み` など初心者向けに補足。
- alerts画面render testとLoop 060 task docを追加。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`

### Result
- lint / typecheck / test / test:integration / build 成功。
- 39 files / 256 tests passed。
- build時にNext.js ESLint plugin未検出warningは出るが、buildは成功。
- alerts画面をローカルブラウザで表示し、未返信チェック、デモ通知、Mock/未接続注意文が見えることを確認。

### Tenant isolation
- tenant resolver、API helper、runtimeは変更なし。
- alerts一覧と操作は既存どおり `x-tenant-id` / `dev_header` 前提を維持。

### External API
- LINE APIなし。
- Slack/email通知なし。
- OpenAI APIなし。
- Supabase接続なし。
- Web crawlなし。

### Risks
- alerts一覧はまだtable表示中心。
- 本物通知、scheduler、alert解決操作は未実装。
- 実ブラウザ目視は人間確認が必要。

### Next
- Loop 057: selectedTenantId transport boundary
- Loop 061: mock/unconnected badge component

## Loop 061: Amami Home internal review edition readiness

### Date
2026-06-15

### What changed
- アマミホーム社内確認版として見せるためのreadiness runbookを追加。
- 社内確認版の定義、確認順、できること/まだできないこと、説明台本、フィードバック項目を整理。
- mock / 一時保存 / 本番未接続 / 開発確認用の表現を社内確認向けに統一。
- README、dev loop docs、local manual checklistから社内確認runbookへ辿れるようにした。
- RoleVisibilityNoteのgeneral variantを初心者向けに補強。
- 社内確認runbookのdocs testとLoop 061 task docを追加。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`

### Result
- `lint` / `typecheck` / `test` / `test:integration` / `build` 成功。
- Vitestは 40 files / 261 tests passed。
- build時に既存のNext.js ESLint plugin未検出warningは出るが、buildは成功。

### Tenant isolation
- tenant resolver、API helper、runtimeは変更なし。
- 社内確認runbookでは `tenant_amamihome` と `x-tenant-id: tenant_amamihome` の開発確認前提を明記。

### External API
- LINE APIなし。
- OpenAI APIなし。
- Supabase接続なし。
- Web crawlなし。
- 本番通知なし。

### Risks
- 社内確認版はまだ一時保存で、API process再起動後はdemo seed再投入が必要。
- 本物LINE送信、OpenAI API、Supabase本番DB、本番ログイン、本番通知、schedulerは未実装。
- 社内確認フィードバックは次Loop以降で小さく分ける必要がある。

### Next
- Loop 057: selectedTenantId transport boundary
- Loop 061.1: internal review feedback triage

## Loop 061.1: internal review feedback triage

### Date
2026-06-15

### What changed
- 社内確認後の意見を直接実装せず、triageして次Loopへ落とす運用docsを追加。
- feedback triage guideでcategory / priority / severity / effortの基準を整理。
- feedback log templateを空で追加し、実顧客情報、LINE userId、APIキー、`.env`、本番ログを書かないルールを明記。
- 社内確認runbook、README、dev loop docsからfeedback記録先へ辿れるようにした。
- internal review runbook testを更新し、triage docsと安全ルールを検証対象に追加。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- `lint` / `typecheck` / `test` / `test:integration` 成功。
- Vitestは 40 files / 263 tests passed。
- docs/testのみの変更のためbuildは未実行。

### Tenant isolation
- tenant resolver、API helper、runtimeは変更なし。
- feedback logではtenantや実顧客を推測できる情報を書かない方針を明記。

### External API
- LINE APIなし。
- OpenAI APIなし。
- Supabase接続なし。
- Web crawlなし。
- 本番通知なし。

### Risks
- 実フィードバックが入るまでは優先度分類は仮運用。
- P0/P1が出た場合は、次Loopで小さく切って優先対応する必要がある。
- 本番化要望を社内確認版の軽微修正と混ぜない運用が必要。

### Next
- Loop 061.2: internal review P0/P1 quick fixes
- Loop 062: AI/RAG copywriting polish

## Loop 062: Amami Home internal review final readiness hardening

### Date
2026-06-15

### What changed
- アマミホーム社内確認版として見せる前の最終確認記録を追加。
- API/Admin起動、demo seed、主要API、RAG source付き回答案、Browser主要画面確認を実施。
- 顧客一覧、顧客詳細、AI/RAG/担当者返信結果、alerts、auth placeholderの表側文言を社内確認向けに小さく補正。
- README、dev loop docs、社内確認runbookからfinal verification docへ辿れるようにした。
- internal review docs testを更新し、final verification docと本番未接続注意を検証対象に追加。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`

### Result
- `git diff --check` 成功。
- lint / typecheck / test / test:integration / build 成功。
- `test` と `test:integration` は 40 files / 264 tests passed。
- buildは10 packages successful。Next.js ESLint pluginの既存警告は出たがbuildは成功。

### Tenant isolation
- API確認は `x-tenant-id: tenant_amamihome` で実施。
- 主要API responseは `tenant_id = tenant_amamihome` を維持。
- runtime repository/APIのtenant分離ロジックは変更なし。

### External API
- LINE APIなし。
- OpenAI APIなし。
- Supabase接続なし。
- Web crawlなし。
- 本番通知なし。

### Browser / API Check
- Browserで `/`、`/customers`、`/customers/customer_demo_yamada_taro`、`/alerts`、auth placeholder 4画面を確認。
- APIはhealth、demo seed、customers、customer detail/timeline、AI summary/draft、RAG search/answer draft、staff reply、alerts check/notifyを確認。
- RAGは `オンライン相談` と `メンテナンス` でsource付き確認。

### Risks
- 社内確認版はまだ一時保存で、API process再起動後はdemo seed再投入が必要。
- 本物LINE送信、OpenAI API、Supabase本番DB、本番ログイン、本番通知、schedulerは未実装。
- 社内確認でP0/P1 feedbackが出た場合は次Loopで小さく優先対応する必要がある。

### Next
- Loop 061.2: internal review P0/P1 quick fixes
- Loop 063: staff reply safety confirmation plan

## Loop 063: staff reply safety confirmation plan

### Date
2026-06-15

### What changed
- 担当者返信を将来本物LINE送信へ接続する前の安全確認設計をdocs化。
- AI下書き、担当者返信、本物LINE送信の責務分離を整理。
- 送信前確認UI、デモ送信/本番送信表示、誤送信防止、二重送信防止、送信失敗時表示、権限/tenant/認証、timeline/audit方針を整理。
- 社内確認runbook、README、dev loop docs、Loop 063 task doc、docs testを更新。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- `git diff --check` 成功。
- lint / typecheck / test / test:integration 成功。
- `test` と `test:integration` は 40 files / 265 tests passed。
- docs/test中心の変更のためbuildは未実行。

### Tenant isolation
- runtime/API/repositoryは変更なし。
- 本番LINE送信前にcustomer tenant、staff tenant、LINE user tenantの整合性確認が必要であることをdocs化。

### External API
- LINE APIなし。
- OpenAI APIなし。
- Supabase接続なし。
- Web crawlなし。
- 本番通知なし。

### Risks
- 送信前確認UI、idempotency、audit log、実LINE provider staging、認証/権限接続は未実装。
- 本物LINE送信へ進む前に、今回の安全設計を小さい実装Loopへ分割する必要がある。

### Next
- Loop 064: staff reply confirmation UI placeholder
- Loop 065: staff reply send safety boundary tests

## Loop 064: staff reply confirmation UI placeholder

### Date
2026-06-15

### What changed
- 顧客詳細の担当者返信カードに送信前確認UI placeholderを追加。
- 返信文入力後に `送信前に確認する` を押し、宛先、利用先、本文、デモ用、本物LINE未送信を確認するカードを表示。
- checkbox確認後だけ `この内容でデモ保存する` を押せるようにした。
- 既存のstaff reply Server Action / API route / `MockLineClient` 挙動は維持。
- README、社内確認runbook、safety design doc、Loop 064 task doc、testを更新。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`

### Result
- `git diff --check` 成功。
- lint / typecheck / test / test:integration / build 成功。
- `test` と `test:integration` は 40 files / 266 tests passed。
- buildは10 packages successful。Next.js ESLint pluginの既存警告は出たがbuildは成功。
- Browser確認はBrowser URL policyで `localhost:3000` がblockされたため未実行。
- 代替確認として、ローカルAPIでstaff replyのデモ保存とtimeline反映を確認。

### Tenant isolation
- 顧客詳細から利用先IDを確認カードへ表示。
- API/runtime/repositoryのtenant分離ロジックは変更なし。

### External API
- LINE APIなし。
- OpenAI APIなし。
- Supabase接続なし。
- Web crawlなし。

### Risks
- まだ本物LINE送信ではない。
- idempotency、audit log、実LINE provider staging、認証/権限接続は未実装。

### Next
- Loop 065: staff reply send safety boundary tests

## Loop 065: Supabase persistence staging plan

### Date
2026-06-15

### What changed
- 社内確認版のin-memory / 一時保存問題を解消するためのSupabase staging persistence計画をdocs化。
- 既存Supabase repository、現在のruntime、データ別保存状況、永続化優先順位を棚卸し。
- local / staging / production分離、env/key管理、migration apply前チェックリスト、RLS/service role方針を整理。
- README、dev loop docs、社内確認runbook、Supabase staging checklist、docs testを更新。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- `git diff --check` 成功。
- lint / typecheck / test / test:integration 成功。
- `test` と `test:integration` は 41 files / 272 tests passed。
- docs/testのみの変更のためbuildは未実行。

### Tenant isolation
- runtime/API/repositoryは変更なし。
- staging計画ではcustomers/messages/alerts/knowledge_pagesをtenant_idで先に分離する方針を明記。
- service role利用時もrepository側tenant_id filter testを維持する方針を整理。

### External API
- LINE APIなし。
- OpenAI APIなし。
- Supabase接続なし。
- Web crawlなし。
- `.env` 作成・変更なし。

### Risks
- 実DBへのmigration applyはまだ未実行。
- runtimeは引き続きin-memoryで、API process再起動後はdemo seed再投入が必要。
- RLS SQL、Supabase Auth/JWT、runtime switch、production dev_header rejectionは未実装。

### Next
- Loop 066: Supabase staging env readiness checklist

## Loop 066: Supabase staging env readiness checklist

### Date
2026-06-15

### What changed
- Supabase stagingへ接続する前のenv / key / project / migration / dummy data readiness checklistをdocs化。
- 既存コードで参照している `SUPABASE_URL`、`SUPABASE_ANON_KEY`、`SUPABASE_SERVICE_ROLE_KEY`、`SUPABASE_DB_URL` の用途と露出可否を整理。
- staging / production project分離、`.env` 運用、migration apply前確認、dummy seed方針、runtime switch前条件、tool readinessを整理。
- README、dev loop docs、Supabase staging persistence checklist、社内確認runbook、docs testを更新。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- `git diff --check` 成功。
- lint / typecheck / test / test:integration 成功。
- `test` と `test:integration` は 41 files / 277 tests passed。
- docs/testのみの変更のためbuildは未実行。

### Tenant isolation
- runtime/API/repositoryは変更なし。
- staging readinessではdummy dataだけを使い、実顧客情報やLINE userIdを使わない方針を明記。
- service role利用時もserver-side限定、tenant_id filter test維持を前提にした。

### External API
- LINE APIなし。
- OpenAI APIなし。
- Supabase接続なし。
- Web crawlなし。
- `.env` 作成・変更なし。

### Risks
- 実DBへのmigration applyはまだ未実行。
- staging project / key / project refはまだ未設定。
- runtimeは引き続きin-memory。
- RLS SQL、Supabase Auth/JWT、runtime switch、production dev_header rejectionは未実装。

### Next
- Loop 067: Supabase runtime switch boundary for customers/messages

## Loop 067: Supabase runtime switch boundary for customers/messages

### Date
2026-06-15

### What changed
- customers/messages向けにruntime mode、repository bundle、factory境界を追加。
- default runtimeはin-memoryのまま維持。
- Supabase modeはfake client注入または明示env boundaryでのみ組み立てる形にした。
- Supabase env不足時はenv名だけを含む `supabase_runtime_not_configured` errorに変換し、secret値を出さない。
- README、dev loop docs、Supabase staging runbook、Loop 067 task doc、testを更新。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`

### Result
- lint / typecheck / test / test:integration / build 成功。

### Tenant isolation
- runtime/APIはまだin-memoryで、既存tenant分離挙動は変更なし。
- Supabase repositoryへ接続する境界はcustomers/messagesに限定。

### External API
- LINE APIなし。
- OpenAI APIなし。
- Supabase接続なし。
- Web crawlなし。
- `.env` 作成・変更なし。

### Risks
- API runtimeはまだSupabase repositoryへ接続していない。
- staging project / env / dummy seedは未設定。
- migration apply、RLS SQL、Supabase Auth/JWTは未実装。

### Next
- Loop 068: Supabase repository integration tests with fake client

## Loop 068: Supabase repository integration tests with fake client

### Date
2026-06-15

### What changed
- `SupabaseCustomerRepository` / `SupabaseMessageRepository` 向けのfake client integration testsを追加。
- customer/messageのtenant_id filter、write payload mapping、timeline order、latest message取得を確認。
- Supabase repository errorでraw message/details/hintを外へ出さず、secret値やURL値を表示しないようにした。
- runtime bundleからfake clientでSupabase customer/message repositoriesを利用できることを確認。
- README、dev loop docs、Supabase staging runbook、Loop 068 task doc、docs testを更新。

### Commands
- `git status --short`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`

### Result
- lint / typecheck / test / test:integration / build 成功。

### Tenant isolation
- customers/messagesのread queryで `tenant_id` filterを確認。
- customer/message write payloadに `tenant_id` が入ることを確認。
- fake resultに別tenant rowが混ざっても返却されないことを確認。

### External API
- LINE APIなし。
- OpenAI APIなし。
- Supabase接続なし。
- Web crawlなし。
- `.env` 作成・変更なし。

### Risks
- 実DBでのrepository integration testは未実施。
- API runtimeはまだSupabase repositoryへ接続していない。
- staging project / env / dummy seedは未設定。
- migration apply、RLS SQL、Supabase Auth/JWTは未実装。

### Next
- Loop 069: staging migration dry-run record

## Loop 069: Codex Development Kit scaffold

### Date
2026-06-15

### What changed
- Codex Development Kit風の5レイヤーscaffoldをMarkdown中心で追加。
- `AGENTS.md` に作業フォルダー制限、`/tmp` 禁止、default in-memory維持、外部接続禁止、push禁止を追記。
- `skills/`、`hooks/README.md`、`subagents/`、`plugins/README.md` を追加。
- `docs/15_runbooks/codex_development_kit.md` とLoop 069 task docを追加。
- runtime/API/product挙動は未変更。
- hooks/pluginsは未有効化。

### Commands
- `pwd`
- `git status --short`
- `git log --oneline -5`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`

### Result
- `git diff --check` 成功。
- lint / typecheck / test / test:integration / build 成功。
- `test` と `test:integration` は 44 files / 302 tests passed。
- buildは10 packages successful。Next.js ESLint pluginの既存警告は出たがbuildは成功。

### Tenant isolation
- runtime/API/DBは変更なし。
- default in-memory runtimeを維持。

### External API
- LINE APIなし。
- OpenAI APIなし。
- Supabase接続なし。
- Web crawlなし。
- `.env` 作成・変更なし。

### Risks
- hooks/pluginsはまだ構想と未有効化READMEのみ。
- 実行hookやplugin配布を行う場合は別Loopでdry-run設計が必要。

### Next
- Loop 070: staging migration dry-run record

## Loop 070: staging migration dry-run record

### Date
2026-06-16

### What changed
- Supabase staging migration apply前の静的dry-run記録を追加。
- `packages/db/migrations/0001_initial_schema.sql` のschema inventory、tenant_id index、customers/messages期待値、staff/auth readiness、RLS SQL未実装状態をdocs化。
- `docs/15_runbooks/supabase_staging_migration_dry_run.md` とLoop 070 task docを追加。
- static migration checksを追加し、staging apply前runbookとREADME/dev loopから参照できるようにした。
- Supabase接続、`.env` 作成、migration apply、API runtime switch、git pushは行っていない。

### Commands
- `pwd`
- `git status --short`
- `git status --short --branch`
- `git log --oneline -8`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- `git diff --check` 成功。
- lint / typecheck / test / test:integration 成功。
- `test` と `test:integration` は 45 files / 311 tests passed。
- docs/static testのみの変更のためbuildは未実行。

### Tenant isolation
- migration上のtenant-owned tables、tenant scoped unique/index、customers/messages repository expectationsを静的に確認。
- runtime/APIは変更なしで、default in-memoryを維持。

### External API
- LINE APIなし。
- OpenAI APIなし。
- Supabase接続なし。
- Web crawlなし。
- `.env` 作成・変更なし。

### Risks
- migrationはまだ実DBへ適用していない。
- RLS SQLは未実装。
- Supabase staging project / key / `.env` は未設定。
- API runtimeはまだin-memory。
- Loop 069 / 070は未push。

### Next
- Loop 071: Supabase staging migration apply plan

## Loop 071: Supabase staging migration apply plan

### Date
2026-06-16

### What changed
- Supabase staging migration apply前の承認条件、apply前チェック、禁止コマンド、rollback / recovery方針をdocs化。
- apply後確認項目、Go / No-Go判断、apply結果記録テンプレートを追加。
- staging persistence/env/dry-run runbooksとREADME/dev loopからLoop 071 docsへリンク。
- Supabase接続、`.env` 作成、migration apply、migration SQL変更、API runtime switch、git pushは行っていない。

### Commands
- `pwd`
- `git status --short`
- `git status --short --branch`
- `git log --oneline -10`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- `git diff --check` 成功。
- lint / typecheck / test / test:integration 成功。
- `test` と `test:integration` は 46 files / 319 tests passed。
- docs/static testのみの変更のためbuildは未実行。

### Tenant isolation
- runtime/API/DB schemaは変更なし。
- apply planではstaging限定、dummy dataのみ、RLSなしproduction禁止、tenant_id分離確認を前提にした。

### External API
- LINE APIなし。
- OpenAI APIなし。
- Supabase接続なし。
- Web crawlなし。
- `.env` 作成・変更なし。

### Risks
- migrationはまだ実DBへ適用していない。
- RLS SQLは未実装。
- staging project / key / `.env` は未設定。
- rollback手順は方針のみで、実行可能な手順化は別Loop。
- Loop 069 / 070 / 071は未push。

### Next
- Loop 072: Supabase staging migration apply execution

## Loop 072: GPT-Codex handoff automation scaffold

### Date
2026-06-16

### What changed
- repo状態、最新Loop、最新dev log、README/skills要点を収集する `collect-context` scriptを追加。
- Codexへ貼るprompt下書きを `tmp/dev-loop/` 配下へ生成する `generate-codex-handoff` scriptを追加。
- handoff automation runbook、Loop 072 task doc、integration testを追加。
- Codex自動実行、OpenAI API呼び出し、Supabase接続、commit/push自動化、git pushは行っていない。

### Commands
- `pwd`
- `git status --short`
- `git status --short --branch`
- `git log --oneline -10`
- `node scripts/dev-loop/collect-context.mjs --out tmp/dev-loop/context.md`
- `node scripts/dev-loop/generate-codex-handoff.mjs --loop 073 --title "Sample safe handoff" --mode docs-only --push forbidden --out tmp/dev-loop/next-codex-prompt.md`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`

### Result
- script動作確認成功。`tmp/dev-loop/context.md` と `tmp/dev-loop/next-codex-prompt.md` を生成。
- 生成promptに `git push は絶対に実行しない` と `runtime/API/UI/DB変更禁止` が含まれることを確認。
- `git diff --check` 成功。
- lint / typecheck / test / test:integration / build 成功。
- `test` と `test:integration` は 47 files / 326 tests passed。
- buildは10 packages successful。

### Tenant isolation
- runtime/API/DB schemaは変更なし。
- prompt下書きにはtenant_id分離とScope確認を人間ゲートで残す前提を維持。

### External API
- LINE APIなし。
- OpenAI APIなし。
- Supabase接続なし。
- Web crawlなし。
- `.env` 作成・変更なし。

### Risks
- 生成promptは下書きであり、Loop固有Scopeは人間確認が必要。
- `tmp/dev-loop` 生成物はgit管理しない作業用ファイル。
- Loop 069 / 070 / 071 / 072は未push。

### Next
- Loop 073: Supabase staging migration apply execution gate

## Loop 073: Supabase staging migration apply execution gate

### Date
2026-06-16

### What changed
- Supabase staging migration apply前のGo / No-Go判定docとrunbookを追加。
- 今回は明示許可、staging project確認、productionでない確認、env/key readinessが未提示のためNo-Goとして記録。
- No-Go時の不足情報と、次に人間が用意するものを整理。
- Supabase接続、`.env` 作成、migration apply、migration SQL変更、RLS SQL、API runtime switch、git pushは行っていない。

### Commands
- `pwd`
- `git status --short`
- `git status --short --branch`
- `git log --oneline -12`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`

### Result
- `git diff --check` 成功。
- lint / typecheck / test / test:integration 成功。
- `test` と `test:integration` は 48 files / 332 tests passed。
- docs/static testのみの変更のためbuildは未実行。

### Tenant isolation
- runtime/API/DB schemaは変更なし。
- No-Go gateではstaging/dummy data限定、実顧客情報やLINE userId禁止、RLSなしproduction禁止を再確認。

### External API
- LINE APIなし。
- OpenAI APIなし。
- Supabase接続なし。
- Web crawlなし。
- `.env` 作成・変更なし。

### Risks
- migrationはまだ実DBへ適用していない。
- staging project / project ref / key / `.env` はrepo内には未設定。
- RLS SQLは未実装。
- Loop 069 / 070 / 071 / 072 / 073は未push。

### Next
- Loop 074: Supabase staging migration apply execution with explicit approval
