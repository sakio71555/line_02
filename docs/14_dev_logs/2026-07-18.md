# 2026-07-18 Development Log

## LINE rich menu publication safety hardening

- Made the secure rich-menu ID output path mandatory for the three-menu lifecycle publisher and blocked before any LINE API call when it is absent.
- Preserved compensating deletion behavior and added a sanitized `cleanup_required` result when rollback deletion also fails.
- Kept rich-menu IDs, access tokens, and raw API response bodies out of operator output and documentation.
- Corrected the active copy-based VPS runbook to use the production Admin localhost port `3100`.
- Safety: implementation and validation do not send LINE messages, call OpenAI, connect directly to databases, or change Nginx, DNS, HTTPS, certbot, packages, or schemas.

## LINE rich menu rollback and deployment port consistency

- Added restoration of the pre-publication default rich menu before compensating deletion when lifecycle publication fails.
- Preserved an active new default and reported sanitized cleanup-required state when restoring the previous default is not possible.
- Changed rich-menu ID env output to same-directory atomic replacement so a failed write cannot truncate the previous valid file.
- Unified current VPS systemd, env, Nginx, and preflight artifacts on Admin localhost port `3100` while preserving historical Loop records.
- Added focused integration coverage for rollback order, restoration failure, atomic output failure, file mode, and current deployment artifacts.
- Safety: all LINE behavior is tested with mocks; no LINE message, OpenAI call, database connection, secret output, Nginx runtime change, DNS, HTTPS, or certbot operation is performed.

## Tenant staff management

- Added an owner-only staff management screen for registering, inviting, updating, disabling, re-enabling, and re-inviting tenant staff.
- Added tenant-scoped Admin API routes, audit events, and Japanese operator feedback for the complete invitation lifecycle.
- Added Supabase Auth invitation reconciliation so an interrupted invite can be recovered without creating a duplicate staff identity.
- Added first-login membership activation and excluded unaccepted or unlinked invitations from daily assignment lists.
- Protected the final active owner across the API, domain repository, and atomic PostgreSQL functions.
- Restricted staff management functions to the service role and added tenant, identity, and concurrency checks in the database boundary.
- Added responsive settings UI and focused integration/unit coverage for permissions, tenant isolation, invitation recovery, activation, and owner protection.
- Safety: secret values, invitation links, and provider response bodies are not recorded; LINE messages, OpenAI calls, and customer data changes are not part of this task.

## First staff owner bootstrap

- Confirmed the production tenant has no registered staff and that the empty staff screen reflects the stored state.
- Forced the first tenant staff registration to the administrator role across the UI, API, repository, and atomic database function.
- Added a tenant-level advisory lock so concurrent first registrations cannot create an administrator-free tenant.
- Kept requested roles unchanged from the second registration onward and added focused API and migration coverage.
- Made the empty state tell the operator to register the actual first administrator instead of creating placeholder staff data.
- Safety: no staff identity, LINE message, OpenAI request, customer record, secret value, or raw log is created or exposed by this change.
