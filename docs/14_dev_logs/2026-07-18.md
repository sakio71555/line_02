# 2026-07-18 Development Log

## LINE rich menu publication safety hardening

- Made the secure rich-menu ID output path mandatory for the three-menu lifecycle publisher and blocked before any LINE API call when it is absent.
- Preserved compensating deletion behavior and added a sanitized `cleanup_required` result when rollback deletion also fails.
- Kept rich-menu IDs, access tokens, and raw API response bodies out of operator output and documentation.
- Corrected the active copy-based VPS runbook to use the production Admin localhost port `3100`.
- Safety: implementation and validation do not send LINE messages, call OpenAI, connect directly to databases, or change Nginx, DNS, HTTPS, certbot, packages, or schemas.

## LINE rich menu rollback and deployment port consistency

- Added restoration of the pre-publication default rich menu before compensating deletion when lifecycle publication fails.
- Preserved an active new default and reported sanitized cleanup-required state when restoring the previous default is not possible.
- Changed rich-menu ID env output to same-directory atomic replacement so a failed write cannot truncate the previous valid file.
- Unified current VPS systemd, env, Nginx, and preflight artifacts on Admin localhost port `3100` while preserving historical Loop records.
- Added focused integration coverage for rollback order, restoration failure, atomic output failure, file mode, and current deployment artifacts.
- Safety: all LINE behavior is tested with mocks; no LINE message, OpenAI call, database connection, secret output, Nginx runtime change, DNS, HTTPS, or certbot operation is performed.
