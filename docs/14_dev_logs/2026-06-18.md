# Development Log: 2026-06-18

## Loop 103: production readiness final gate

### Date
2026-06-18

### What changed
- OpenAI real API gateを追加し、`AI_PROVIDER=openai`、key/model、tenant AI settings、RAG source、draft-only/no auto-sendを必須化。
- `OpenAiProvider` をfake transportで検証できる境界にし、OpenAI API実呼び出しなしでResponses API shapeを確認。
- production Auth runtimeの自動構成残差分を監査し、fake client / fake StaffAuthLookupでsafe gateを追加確認。
- LINE real push gateをfinal auditとしてrunbookへ反映。
- production readiness final runbookを追加し、最終判定を `production_no_go` として記録。

### Commands
- `git status --short`
- `git status --short --branch`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`

### Result
- 実行結果はLoop完了報告で確認する。

### Tenant isolation
- OpenAI gateはtenant AI settingsとtenant scoped RAG sourceを必須にする。
- selectedTenantIdは引き続きAPI側active membership再検証後のtenantだけを使う。

### External API
- LINE APIなし。
- OpenAI APIなし。
- production Supabase接続なし。

### Risks
- production runtimeのSupabase Auth client / StaffAuthLookup自動構成は未完了。
- Admin UIの実login/session/token取得は未完了。
- real LINE/OpenAI enablementとproduction deploy/smokeは未実施。

### Next
- Loop 104: production Auth runtime auto wiring plan

## Loop 104: production Auth runtime auto wiring

### Date
2026-06-18

### What changed
- `AUTH_SESSION_VERIFIER=supabase` のproduction Auth runtime factory境界を追加。
- Supabase Auth client境界、`SupabaseAuthSessionVerifier`、StaffAuthLookup境界をserver-sideで構成できるようにした。
- productionでfake verifierをdefault利用せず、`AUTH_SESSION_VERIFIER=fake` を拒否するtestを追加。
- required env不足、invalid config、runtime例外をsecret/token/URLなしでsafe failureするtestを追加。

### Commands
- `git status --short`
- `git status --short --branch`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`

### Result
- 実行結果はLoop完了報告で確認する。

### Tenant isolation
- selectedTenantIdは引き続きactive membership再検証後のtenantだけを使う。
- StaffAuthLookupは `staff_users.auth_user_id` とmembership解決のみを担当する。

### External API
- LINE APIなし。
- OpenAI APIなし。
- production/staging Supabase接続なし。

### Risks
- Admin UIの実login/session/token取得は未完了。
- real LINE/OpenAI smokeとproduction deploy/smokeは未実施。

### Next
- Loop 105: Admin login/session minimal integration

## Loop 105: Admin login/session minimal integration

### Date
2026-06-18

### What changed
- Admin UI向けのsession controller境界を追加し、sign-in / session read / refresh / logoutをfake auth clientで検証。
- Admin API helperへsession token providerを渡す境界を追加。
- login/logout placeholder UIを、実Auth client接続前の境界状態とtoken非保存方針が分かる表示へ更新。
- Loop 105 task docとAdmin login session runbookを追加。

### Commands
- `git status --short`
- `git status --short --branch`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`

### Result
- 実行結果はLoop完了報告で確認する。

### Tenant isolation
- selectedTenantIdはAuthorization tokenとは分離し、引き続きAPI側active membership再検証を前提にする。

### External API
- LINE APIなし。
- OpenAI APIなし。
- Supabase Auth / production DB接続なし。

### Risks
- 実Supabase Auth client注入、real login smoke、production deploy/smokeは未実施。

### Next
- Loop 106: production auth runtime readiness smoke without production connection

## Loop 106: VPS deployment plan and templates

### Date
2026-06-18

### What changed
- `taiyolabel.site` 向けVPS監査結果、planned host/port、No-Go条件をdocs化。
- nginx HTTP bootstrap / SSL template、systemd template、API/Admin env exampleを追加。
- SSL/certbot、secret投入、LINE webhook予定、rollback手順をrunbook化。
- production readiness finalへ、deployment planは追加済みだが実deploy未実施で `production_no_go` 継続と追記。

### Commands
- `git status --short`
- `git status --short --branch`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`

### Result
- 実行結果はLoop完了報告で確認する。

### Tenant isolation
- tenant runtimeやDB schemaは変更なし。production deploymentでもverified tenant contextを維持する前提を記録。

### External API
- VPS SSHなし。
- LINE APIなし。
- OpenAI APIなし。
- Supabase production/staging接続なし。

### Risks
- production start scripts未定義。
- API planned port `8788` は現runtime codeに未接続。
- SSL/certbot/systemd/nginx実配置、production smokeは未実施。

### Next
- Loop 107: production start script and port boundary plan

## Loop 107: production start script and port boundary

### Date
2026-06-18

### What changed
- API/Adminにproduction `start` scriptを追加。
- API production bindingを `API_HOST` / `HOST`、`API_PORT` / `PORT` から解決し、defaultを `127.0.0.1:8788` にした。
- Admin production bindingはNext.js `HOSTNAME=127.0.0.1` / `PORT=3002` としてsystemd/env templateを同期。
- Loop 107 task docとproduction start/port boundary runbookを追加。

### Commands
- `git status --short`
- `git status --short --branch`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`

### Result
- 実行結果はLoop完了報告で確認する。

### Tenant isolation
- tenant runtimeやDB schemaは変更なし。deployment時もverified tenant contextを維持する前提。

### External API
- VPS SSHなし。
- LINE APIなし。
- OpenAI APIなし。
- Supabase production/staging接続なし。

### Risks
- VPS systemd/nginx実配置、SSL/certbot、external smokeは未実施。
- production readinessは引き続き `production_no_go`。

### Next
- Loop 108: VPS deployment preflight dry-run checklist

## Loop 108: VPS dry deployment preflight commands

### Date
2026-06-18

### What changed
- `taiyolabel.site` 向けVPS dry deployment preflight command packを追加。
- read-only audit、backup、release directory、secret env、install/build、local smoke、systemd、nginx、certbot、external smokeの実行順をdocs化。
- rollback runbookとNo-Go checklistを追加。
- production readiness finalへ、preflight docsは追加済みだがVPS実作業未実施で `production_no_go` 継続と追記。

### Commands
- `git status --short`
- `git status --short --branch`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`

### Result
- 実行結果はLoop完了報告で確認する。

### Tenant isolation
- runtime、DB schema、tenant logicは変更なし。preflight docsでもverified tenant contextを維持する前提。

### External API
- VPS SSHなし。
- nginx/systemd/certbot実行なし。
- LINE APIなし。
- OpenAI APIなし。
- Supabase production/staging接続なし。

### Risks
- VPS上でのread-only audit、systemd/nginx実配置、SSL/certbot、external smokeは未実施。
- Admin real login smoke、LINE real smoke、OpenAI real smokeは未実施。

### Next
- Loop 109: VPS dry deployment execution gate

## Loop 109: VPS localhost-only mock deployment execution

### Date
2026-06-18

### What changed
- VPSへ `/var/www/amami-line-crm` を配置し、review/mock envを `/etc/amami-line-crm` に作成。
- `amami-line-crm-api.service` / `amami-line-crm-admin.service` をlocalhost-only review用に作成し、enableせずstartする手順を実施。
- VPS smokeで見つかったAPI review start方式、workspace package exports、Admin hostname指定を最小修正。
- APIはlocalhost-only reviewでは `tsx src/index.ts` で起動し、compiled API runtime整理は後続Loopへ分離。
- Loop 109 task docとlocalhost mock deployment runbookを追加。

### Commands
- `git status --short`
- `git status --short --branch`
- `git diff --check`
- `npx pnpm@10.12.1 lint`
- `npx pnpm@10.12.1 typecheck`
- `npx pnpm@10.12.1 test`
- `npx pnpm@10.12.1 test:integration`
- `npx pnpm@10.12.1 build`

### Result
- 実行結果はLoop完了報告で確認する。

### Tenant isolation
- review/mock起動は `REPOSITORY_RUNTIME=in_memory`。
- production Supabaseには接続しない。

### External API
- Nginx/certbotなし。
- LINE APIなし。
- OpenAI APIなし。
- Supabase production/staging接続なし。

### Risks
- localhost-only reviewであり、Nginx公開、SSL、external smoke、Admin real login smoke、LINE/OpenAI real smokeは未実施。

### Next
- Loop 110: User tunnel review and UI checklist
